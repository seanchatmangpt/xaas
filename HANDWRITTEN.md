# HANDWRITTEN.md — hand-written product-surface ledger

Contract: every hand-written product artifact names its missing capability and
intended owner pack, and the ledger shrinks monotonically per milestone.

Format: `path | semantic element | missing capability | intended owner pack | date`

## Active

lib/xaas/ultracode/no_llm_policy.ex, priv/no_llm/policy.json, lib/xaas/ultracode/semantic_drive.ex (`no_llm_guard/1`, `no_llm_environment/1`, `llm_variables/0` delegating; drive children unset every unadmitted variable), docs/sjira/v26.9.23/courts/no_llm_env.sh (built from the policy data), docs/sjira/v26.9.23/courts/GC23-5.sh (F3 steps 5-6), test/xaas/ultracode/semantic_drive_test.exs (`SemanticDriveNoLlmGuardTest`), test/xaas/sjira/successor_test.exs + test/xaas/ultracode/machine_experience_test.exs (clean env = the admitted projection) | the fail-closed no-LLM guard (lane R1-X-GUARD, GC-26.9.23 GC23-5; PRD PR-009, ARD F3): the permitted court environment is an ALLOWLIST of variable names declared as data (priv/no_llm/policy.json: builder HOME/PATH, caller_or_default MIX_HOME/HEX_HOME/ASDF_DATA_DIR/LANG, caller_if_set MIX_ENV/MIX_TEST_PARTITION/DEV_DB_*, court_assignment TMPDIR, launcher-set BINDIR/EMU/PROGNAME/ROOTDIR/PWD/SHLVL/__CF_USER_TEXT_ENCODING/ASDF_INSTALL_*/MIX_ARCHIVES, drive-set MIX_BUILD_PATH/GIT_*/STATE_DIR/ASDF_*_VERSION); any other variable is REFUSED(llm_credential_present) when one of 27 providers (Anthropic, Claude Code, OpenAI/Codex, Azure OpenAI, Google Gemini/Vertex, Mistral, Groq, OpenRouter, DeepSeek, xAI, Cohere, Together, Fireworks, Perplexity, AWS Bedrock, Ollama, Z.AI/GLM, ZCode, llm, Aider, Cursor, opencode, Hugging Face, Replicate, DashScope/Qwen, Moonshot, LiteLLM) claims its name and REFUSED(unadmitted_environment) otherwise, a provider CLI binary on PATH is REFUSED(llm_credential_present), names only; the module compiles the data (`@external_resource`, malformed data fails the build); no_llm_env.sh reads the same file (python3 -I) and resolves tool symlinks so a shared bin dir (/opt/homebrew/bin: git, python3 beside gemini, codex, ollama, openai) never reaches the court PATH (2026-09-23) | UNSUPPORTED(generator-capability): checked the 326 ggen-marketplace packs (0 files match no_llm/no-llm/llm_credential/NoLlm; the allowlist hits are tool/permission allowlists, e.g. claude-code-pack, invariant-gate-pack) and ggen_igniter semantic-jira-pack (its own GC23-1 helper scripts/sjira/bootstrap_court.sh keeps a separate denylist); xaas ggen.toml renders only the CASTLE bridge pack | a no-LLM execution-environment pack (policy ontology -> guard module + court env builder + provider table test), then this module and the sh parser retire; ggen_igniter bootstrap_court.sh should consume the same data | 2026-09-23

lib/xaas/sjira/successor.ex, lib/mix/tasks/xaas.successor.ex, scripts/successor_work_graph.exs, docs/sjira/v26.9.23/successor/intake/*.json (their output) | GC23-12 successor intake (lane V23-H; PRD section 12, ARD sections 5.5, 9, 24 M9): the compiled successor orders (`mix semantic_jira.compile_prose` output) projected into kernel work-order rows through the semantic-jira-pack's own bootstrap/work_orders.rq + dependencies.rq (`Bootstrap.Graph.orders/3`, run as `mix run --no-start` in GGEN_IGNITER_DIR) joined with the contract tuple (the one hand step), then the SAME entry the no-LLM drive uses -- `mix semantic_jira.frontier` (empty ledger) -> first eligible order -> `mix semantic_jira.descriptor --provider recipe` -> `Xaas.Sa2a.Route.tuple/2` + `Route.resolve/1` -- with `classify/1` typing an unregistered capability `UNSUPPORTED(provider_capability)` (a registered one KNOWN-routable, `construct:unassigned` UNKNOWN) as a successor item instead of the drive's refusal, the Route digest cross-checked against the pack's tuple digest, canonical byte-stable outputs and `--check` recompute (2026-09-23) | UNSUPPORTED(generator-capability): checked the 326 ggen-marketplace packs (frontier-release-factory-pack renders a working-backwards press release and acceptance contract FROM a release graph -- the inverse of the first mile; counterfactual-frontier-replay-pack / consumer-realization-frontier-pack render static frontier assertions, no work-order row projection or capability resolver) and ggen_igniter semantic-jira-pack at int 3937a4f (bootstrap/work_orders.rq selects the kernel fields but the pack emits no kernel+tuple work-graph JSON and no capability->provider typing) | semantic-jira-pack (a `mix semantic_jira.work_graph` projection of compiled orders to kernel+tuple rows; then the script retires), ultracode-actuation-lease-pack (capability->provider registry typing, UNSUPPORTED(provider_capability) as a registry query) | 2026-09-23

docs/sjira/v26.9.23/successor/goal.ttl | the GC-26.9.24 successor goal graph (V23-H): root v23:GC-26.9.24 pinning the successor prose sha256 + the four gates GC24-0..GC24-3 the prose declares ("GC24-<n>, <title>: ..."), no WorkOrder (every GC-26.9.24 order is compile_prose output) (2026-09-23) | UNSUPPORTED(generator-capability): no pack renders a GoalCheckpoint graph from the gate lines of a WBPR (semantic-jira-pack templates/wbpr.md.eex and frontier-release-factory-pack render prose FROM a graph); the compiler's gate-coverage rule (prose/uncovered_gates.rq) and the GC23-12 successor law (L1/L3) bound this residue | semantic-jira-pack (a prose -> GoalCheckpoint gate extractor over sj:Proposition kinds, admitted like candidates) | 2026-09-23

docs/sjira/v26.9.23/courts/GC23-12.sh, docs/sjira/v26.9.23/courts/successor_law.py, docs/sjira/v26.9.23/courts/successor_acceptance.sh, lib/mix/tasks/xaas.stop_court.ex (`blocked_exit/0` = 77, `blocked_line/1`, typed `BLOCKED:<reason>` gate standing + `standing.broken_term`), test/sjira/v26_9_23_goal_test.exs, test/xaas/sjira/successor_test.exs | the GC23-12 court body (V23-H; path and exit contract from V23-P, extended with the stop court's typed BLOCKED exit): committed/unmodified successor artifacts, prose_spans.py check --extract, compile_prose --check, the rdflib successor law (no goal WorkOrder, order IRI = WO-sha256(subject)[:16], full tuple, placement under GC-26.9.24 only, predecessor inventory, intake cross-check) with two anti-vacuity mutations, `mix xaas.successor --check`, F3 refusal, the acceptance probe fired both ways on a scratch git repo, then the operator-acceptance edge (BLOCKED(operator_acceptance), broken_term R_missing_authority, exit 77, until the operator commits docs/sjira/v26.9.23/successor/ACCEPTED naming the prose sha256) (2026-09-23) | UNSUPPORTED(generator-capability): same as the GC23-4..8 court row (no pack renders a court body from a gate's acceptance/falsifier IRIs) | semantic-jira-pack (court-body projection) | 2026-09-23

lib/xaas/ultracode/semantic_drive.ex, lib/xaas/ultracode/semantic_drive/episode.ex, lib/xaas/ultracode/semantic_drive/ocel.ex, lib/mix/tasks/xaas.episode.ex, scripts/sa2a_route_task.exs | the first no-LLM KNOWN episode (lane V23-D, GC-26.9.23 GC23-4..GC23-8; PRD PR-008..PR-012, ARD §8, §10, §13, §14, §18): `SemanticDrive.drive/1` chains `mix semantic_jira.frontier` -> Route tuple -> capability resolution -> SA2A task (`scripts/sa2a_route_task.exs` = gen_sa2a_route_fixture.exs's projection + `workOrder`/`evidenceHorizon`) -> `mix semantic_jira.descriptor --provider recipe` -> epoch contract (descriptor + the SA2A-carried tuple) + `Route.conserve/3` -> `SemanticWork.materialize/2` (binding :graph pinned to the SA2A admitted snapshot digest) -> `RecipeWorker` -> `SemanticReceipt.export/1` -> independent `Verifier.run/2` on a fresh worktree + revert falsifier -> `RProjection.write/2` -> `mix semantic_jira.xaas_receipt`/`reconcile` -> frontier, recording the Route tuple digest AND the ARD §8 SemanticExecutionRequest digest at five hops (`verify_hops/1` replays both), typed refusals with a Chatman broken term at every hop, the no-LLM guard (F3), OCEL observations rendered in the xaas court vocabulary and the OCEL 2.0 standard JSON (`Ocel.equivalent?/2`); `Episode.prepare/1` (deterministic drift commit by git plumbing, two-order work graph + court maps, fresh ledger); the task's runtime registration (repo alias, worktree root, `court_suite/0` = the `ggen-igniter-format` TargetSuites declaration with its `~/.asdf` PATH bound to ASDF_DATA_DIR because the F3 court HOME is fresh) (2026-09-23) | UNSUPPORTED(generator-capability): checked the 326 ggen-marketplace packs -- ggen-ecosystem-ocel-pack renders an OCEL log of one ggen manufacturing run from its own U/G/O/Q/M run facts (no ARD §13 classes, no runtime observation input), ocel-feedback-pack consumes cnv-ocel signals, receipt-provenance-unification-pack renders a receipt validator/contract matrix, sa2a-bridge-pack a stdio port bridge; none renders a multi-process episode driver, a hop-digest recorder or a drift-subject preparer -- and ggen_igniter semantic-jira-pack at fri/FRI-T6 bc8ea06 (its descriptor emitter carries no tuple, `task_from_work_order/2` no tuple projection) | semantic-jira-pack (emit the ARD §8 request tuple in the descriptor bridge + SA2A task; then the contract merge and the SA2A script retire), ggen-ecosystem-ocel-pack (admit the ARD §13 classes + a runtime-observation input), ultracode-actuation-lease-pack (episode driver shapes) | 2026-09-23

docs/sjira/v26.9.23/courts/GC23-4.sh, GC23-5.sh, GC23-6.sh, GC23-7.sh, GC23-8.sh, docs/sjira/v26.9.23/courts/no_llm_env.sh | the GC23-4..GC23-8 court bodies over the committed reference episode docs/sjira/v26.9.23/episodes/fmt-1 (V23-D; path and exit contract from V23-P: 0 ALIVE, 75 UNKNOWN, else refused): route_test + independent python3 recompute of both hop digests re-derived from the carrier artifacts + `--verify-hops` + two F2 mutations (GC23-4); executor/OCEL provider values + no-LLM env control + F3 exposed-key rerun (GC23-5); fresh shared clone, pinned-toolchain `mix format --check-formatted` at the receipt head, F4 revert falsifier and the undrifted subject (GC23-6); validate_receipt.py + `mix xaas.ocel_validate` + `pm4py.read_ocel2_json` + rendering equivalence + reached ARD §13 classes (GC23-7); recorded frontier diff + live `mix semantic_jira.frontier` recompute in GGEN_IGNITER_DIR under a private MIX_BUILD_PATH (GC23-8); the F3 environment builder (`env -i`, fresh HOME, PATH from the mix/erl/git/python3 dirs refusing any holding claude/zcode) (2026-09-23) | UNSUPPORTED(generator-capability): the V23-P court scripts are themselves hand-written residue (goal.ttl names only the command); no pack renders a court body from a gate's acceptance/falsifier IRIs (gate-vacuity-court-pack and semantic-gate-witness-court-pack judge gate outputs, they do not render shell courts) | semantic-jira-pack (court-body projection from sj:acceptance/sj:falsifier + a witness map) | 2026-09-23

lib/xaas/ultracode/machine_experience.ex, lib/xaas/ultracode/machine_experience/exploration.ex, lib/xaas/ultracode/machine_experience/episode.ex, lib/mix/tasks/xaas.machine_experience.ex, scripts/machine_experience.exs, lib/xaas/ultracode/semantic_drive.ex (`:route` provenance on `CapabilityResolved`, `steps/0`, `graph_side/3`), lib/xaas/ultracode/semantic_drive/ocel.ex (`extension_classes/0` RouteDecided/ExplorationStarted/ExplorationCompleted, `court_form/2`/`standard_form/2` event-type declaration, `from_court_form/1`) | the MachineExperience ratchet (lane V23-M, GC-26.9.23 GC23-9; PRD PR-014, PR-016, ARD §13, §15): router (KNOWN from a declared capability, else from an ADMITTED `xme:AdmittedExperience` -> present `sj:MachineExperience` whose SPARQL applicability predicate selects the order's RDF rendering and whose consequence bounds contain it; the route names the experience IRI and takes the capability from `xme:admittedCapability`; else UNKNOWN / ambiguous UNKNOWN), deterministic generalization of the ARD §15 fields from the source order + drive evidence, the admission gate (closed field set, CANDIDATE/NONE, digests, graph-representable record whose ggen_igniter experience digest recomputes, typed admission fields, ARD fields, canonical capability, decodable bounds, self-applicability evaluated by the typed `applicability/2` -- an unevaluable predicate is `REFUSED(predicate_unevaluable)` at admission and `REFUSED(experience_predicate_unevaluable)` at routing, never 'does not apply' -- bounds, ALIVE + independent pass + revert killed, and the source exploration bound by digest and within budget; the admission digest v2 binds the ARD fields AND the whole record), routing-time re-admission from the graph (`admissions/1`: every ADMITTED node is re-verified -- record checks, both digests recomputed, the two nodes closed over their rendering -- and one that does not verify refuses the route, `REFUSED(experience_admission_invalid)`, never dropped; repair 2), the ggen_igniter experience-digest formula bound in xaas (`experience_digest/1`; ggen_igniter is not a compile-time dependency of xaas -- the hex release it pins has no SemanticJira -- so the formula is qualified against the real `SemanticJira.digest/1` by admission, the e2e test and the GC23-9 court), sorted Turtle rendering, the PR-016 exploration budget (time_s, drive_runs, candidates, consequence_ceiling, evidence_requirement; metered before each candidate drive by `next/2` and after it by `within_budget/2`; exhaustion, including a drive that overruns time_s, = receipted UNKNOWN naming any driven head), the episode runner (prepare = V23-D's drift subject with `requires_capability` removed + `failure_class`; run = route -> drive / exploration -> ggen_igniter `SemanticJira.machine_experience/1` + SHACL court -> composed OCEL), the fleet-R-shaped UNKNOWN receipt; the xme: vocabulary (`xme:failureClass`, `xme:AdmittedExperience`, `xme:problemClass` ... `xme:admissionDigest`) is declared nowhere but this code because semantic-jira-pack (V23-C-owned, reserved) has no ARD §15 properties and its `sj:MachineExperienceShape` is closed over the ggen_igniter fields (2026-09-23) | UNSUPPORTED(generator-capability): checked the 326 ggen-marketplace packs -- experience-projection-pack models MX *projections of capabilities* (discoverable/composable/verifiable/replayable booleans), not episode-derived routing experience; sa2a-bridge-pack names autofde-lab's MachineExperienceCompiler (a Python port edge with LLM fallback, not a deterministic router); dfcm-explore-* packs render candidate-space ontologies, no budget meter; no pack renders a SPARQL applicability router, an admission gate or an exploration budget, and ggen_igniter semantic-jira-pack at c90c17b renders `machine_experience/1` only (no ARD §15 fields, no router) | semantic-jira-pack (ARD §15 properties on sj:MachineExperience + an applicability-router query/template; then xme: retires), experience-projection-pack (episode-derived routing experience), ggen-ecosystem-ocel-pack (the extension event classes) | 2026-09-23

docs/sjira/v26.9.23/courts/GC23-9.sh | the GC23-9 court body (V23-M; path and exit contract from V23-P: 0 ALIVE, 75 UNKNOWN, else refused): fleet validator on me-1's UNKNOWN receipt and both drives' R projections; rdflib + pm4py over the committed episodes me-1/me-2 (UNKNOWN before exploration, bounded exploration digest-bound to route and admission, ALIVE candidate drive, the admitted Turtle with every ARD §15 field, OCEL counts; the ratchet: me-2 0 LLM-provider / 0 exploration events vs me-1's non-zero (detector anti-vacuity), strictly fewer events and steps, route + RouteDecided + CapabilityResolved citing the experience IRI, rdflib evaluation of the admitted predicate over me-2's order.ttl); ggen_igniter's SHACL court live in GGEN_IGNITER_DIR (private MIX_BUILD_PATH) + a promoted-to-ALIVE mutation it must refuse; `mix xaas.ocel_validate`; live `--route` KNOWN, F3 refusal, and the falsifier (experience or admission removed -> UNKNOWN; an unevaluable admitted predicate -> REFUSED(experience_predicate_unevaluable), exit 3; the runner refuses to execute) (2026-09-23) | UNSUPPORTED(generator-capability): same as the GC23-4..8 court row (no pack renders a court body from a gate's acceptance/falsifier IRIs) | semantic-jira-pack (court-body projection) | 2026-09-23

test/xaas/ultracode/machine_experience_test.exs | Chicago qualification of the ratchet (V23-M): real RDF.ex/SPARQL.ex over real rendered graphs (predicate selection/rejection, bounds, admission refusals, router KNOWN by IRI / UNKNOWN after removing the experience or its admission / ambiguous / graph-driven capability), the exploration budget, OCEL court-form round trip on the committed fmt-1 log + extension-class validation, real `mix xaas.machine_experience --route` processes under no_llm_env.sh (UNKNOWN 4, KNOWN 0, F3 3); the typed applicability errors, the unevaluable-predicate route refusal, the exploration-budget admission refusals and `within_budget/2`, the runner-level F3 guard (`Episode.run/1` with a credential variable or an LLM binary on PATH); and, with GGEN_IGNITER_DIR, the two-episode ratchet end to end on a real `git clone --local` subject based at the committed episodes' recorded base (real drive, real ggen_igniter `machine_experience/1` + SHACL, fleet validator, pm4py), a candidate that resolves only by overrunning time_s (receipted UNKNOWN, no experience), the drive's `route_capability_mismatch` refusal, and a budget-exhausted exploration; repair 2: the admission refusals for an edited record / unsorted refs / list-valued field / undecodable bounds, every routing-time edit class of an admission (capability, predicate, bounds, verification, problem class, subject, standing, work-order digest, second value, extra triple, verdict, missing falsifiers) refused with its named check, an unverifiable admission beside a sound one refused (never dropped), a sealed unevaluable predicate refused, the CLI over an edited and a sealed graph, and e2e the xaas experience digest equal to the real `machine_experience/1` digest plus the runner refusing an edited admission (2026-09-23; repair 1 2026-09-23; repair 2 2026-09-23) | no admitted gate pack for episode qualification | ultracode-actuation-lease-pack gates/ | 2026-09-23

lib/xaas/sa2a/route.ex (`admit_epoch/1`: `{:refused_semantic_work, {:invalid, :capability}}` -> `{:invalid_field, "capability"}`) | integration repair (V23-D): after FRI-T2's `SemanticWork.optional_capability/1` merged beside FRI-T4's route, a GALL label at the epoch hop was refused as an opaque `{:epoch_contract, _}` instead of the contract's invalid capability field -- 16 route_test failures at friday/gc-fri-0800 b3f85b3 (route.ex/route_test.exs/semantic_work.ex byte-identical to that base), 74/74 after (2026-09-23) | same as the route.ex row below (no generator renders the per-representation field map) | ggen-marketplace sa2a-bridge-pack (route-conservation extension) | 2026-09-23

test/xaas/ultracode/semantic_drive_test.exs | Chicago qualification of the drive (V23-D): the full drive over a real `git clone --local` of the graph checkout's repository with a real prepared episode, real `mix semantic_jira.*` processes in GGEN_IGNITER_DIR (private APFS clone of its `_build/test`), real sandboxed Postgres, the real recipe provider, fabric court, independent verifier, fleet validator (python3) and pm4py; typed refusals for an unregistered capability, an exposed credential / a real `claude` executable on PATH / a real `mix xaas.episode --check-env` subprocess under no_llm_env.sh, a no-drift subject (no_delta), live and replayed digest mutations, and `Episode.prepare/1` determinism + diverged-branch refusal (2026-09-23) | no admitted gate pack for episode qualification | ultracode-actuation-lease-pack gates/ | 2026-09-23

lib/xaas/ultracode/semantic_drive.ex (`graph_toolchain/2`, `BUILD_BROKEN graph_side_build_broken` at the first graph-side process), lib/mix/tasks/xaas.episode.ex (`--graph-toolchain`), docs/sjira/v26.9.23/courts/GC23-8.sh (live recompute via that resolution; compile failure -> UNKNOWN), test/xaas/ultracode/semantic_drive_test.exs (`SemanticDriveToolchainTest`) | graph-side toolchain resolution (V23-D B1b continuation, PRD PR-012, ARD section 14): the Elixir that compiled the build under judgement (`<build>/lib/<app>/.mix/compile.elixir_scm` = {elixir, otp}) on the running ERTS, else the `.tool-versions` pin; observed need: ggen_igniter-int c90c17b `_build/test` was compiled by Elixir 1.19.5/OTP 28 while its pin is 1.18.4-otp-27, so the pinned drive recompiled 97 files and the rustler NIF (`cargo` enoent) and 3 drive tests refused at the sjira hop (2026-09-23) | UNSUPPORTED(generator-capability): no ggen-marketplace pack (326 checked by grep for compile.elixir_scm / tool-versions / elixir_toolchain: 0 generator matches) and no ggen_igniter pack renders a toolchain resolver; `RecipeWorker.toolchain/1` (the pin half, reused) is itself hand-written | ultracode-actuation-lease-pack (toolchain identity shapes + resolver template) | 2026-09-23

lib/xaas/ultracode/semantic_replay.ex, lib/mix/tasks/xaas.replay.ex, scripts/semantic_replay_task.exs, docs/sjira/v26.9.23/courts/GC23-10.sh, docs/sjira/v26.9.23/courts/replay_project.py, docs/sjira/v26.9.23/episodes/fmt-1/replay.json, lib/xaas/ultracode/semantic_crown.ex (`replay/2` public + directory-aware `copy_into/2`, optional `:ggen_build_path`/`:ggen_path`), test/xaas/ultracode/semantic_replay_test.exs | cold replay (lane V23-R, GC-26.9.23 GC23-10; PRD PR-013, ARD §7, §16, F5, F6): `SemanticReplay.replay/1` rebuilds subject identity (git refs: receipt head reachable, descended from base, ancestor of the subject ref tip, `git diff --name-only head tip -- <path_scope>` empty), evidence (`SemanticReceipt.receipt_digest/1` recomputes; `mix semantic_jira.xaas_receipt` maps each sealed receipt; `SemanticJira.digest/1` via `mix run scripts/semantic_replay_task.exs digest` binds it to the committed event), standing/completed/frontier (`mix semantic_jira.reconcile` re-derives the TransitionLog into a fresh ledger -- byte-identical to the committed one for fmt-1 -- then `mix semantic_jira.frontier`), `SemanticJira.replay_check/2` (KNOWN_REPLAY) via the same script, typed divergence with Chatman broken terms, canonical JSON + sha256 digest; the court (two cold runs under no_llm_env.sh inside a sandbox-exec profile denying ~/.claude, F3/F5/F6 + out-of-scope control) and the independent python3 projector of the episode-time state (its output is the recorded digest replay.json); the crown replay copied a directory ledger with `File.cp!/2` (`:eisdir`) (2026-09-23) | UNSUPPORTED(generator-capability): checked the 326 ggen-marketplace packs -- counterfactual-frontier-replay-pack renders static candidate/receipt assertions from SPARQL rows (no ledger re-derivation, no git-ref currency), ggen-igniter-bootstrap-pack a mix project skeleton, evidence-standing-pack a standing-chain checker -- and ggen_igniter semantic-jira-pack at friday/gc-fri-0800 c90c17b (no replay task renders; `replay_check/2` and `SemanticJira.digest/1` have no mix task, hence the script) | semantic-jira-pack (a `mix semantic_jira.replay` task over receipts + ledger + graph + refs, then this module and the script retire), ultracode-actuation-lease-pack (replay court shapes) | 2026-09-23

lib/xaas/ultracode/recipe_worker.ex, config/config.exs (`:ultracode_construction_recipes` "recipe:mix-format", `:ultracode_engine_worker`, `:ultracode_engine_providers`), lib/xaas/ultracode/engine.ex (provider allowlist) | deterministic recipe provider (FRI-T2, repaired by V23-T2R, GC-26.9.23; PRD PR-009/PR-010/PR-011, ARD §9-§10): name-only recipe resolution + admission (literal argv, `elixir_toolchain: :target` without a pinned PATH), directed `Lease.claim_next/3`, steps through `Verifier.spawn_and_collect/6`, lease renew after every passing step with HALT on loss (`{:error, {:lease_lost, reason}}`, no further step/staging/commit), the delta staged in a private `GIT_INDEX_FILE` + `git commit-tree` and published only by the lease FENCE (`git update-ref HEAD <commit> <base_head>` inside a `Xaas.Repo` transaction holding the epoch row `FOR UPDATE` while the token is bound, `:running` and unexpired; repair round 1), `Lease.close/4`, token redaction of every returned error and logged Lease error (repair round 1), `toolchain/1` (target `.tool-versions` via the asdf install layout, else the running node's `mix` + ERTS; an existing but unreadable pin file is a logged `:toolchain_unresolved` refusal, repair round 1) recorded as `toolchain` evidence; the engine's provider allowlist returning `{:error, {:invalid_config, :ultracode_engine_providers}}` (logged) for a malformed value (2026-09-23) | UNSUPPORTED(generator-capability): checked the 325 ggen-marketplace packs (no ultracode/lease/recipe/provider-registry pack; `xaas-ash-core-pack` renders governance changes/validations only, `ash-runtime-integration-contract-pack` contracts only), `mix ash.gen.*` (no worker/lease-protocol generator) and ggen_igniter semantic-jira-pack on friday/gc-fri-0800 (`sj:requiresCapability`/`sj:capabilityId` name the capability but no template renders its provider) | ultracode-actuation-lease-pack (recipe-provider + provider-allowlist + toolchain-resolution shapes promoted from this module), semantic-jira-pack (capability -> provider registry projection) | 2026-09-23

priv/repo/migrations/20260922230000_add_capability_id_to_ultracode_runs.exs, priv/resource_snapshots/repo/ultracode_runs/{20260923074101,20260923074120}.json | `Run.capability_id` column: body GENERATED by `mix ash_postgres.generate_migrations --domains Xaas.Ultracode --name add_capability_id_to_ultracode_runs` (bytes unchanged, sha256 126c83e3...; the FRI-T2 hand-written `change/0` body was deleted), snapshots generated (baseline `--snapshots-only` at friday/gc-fri-0800 41679c6 acknowledging court_map + semantic_bridge, then the capability_id snapshot); the only hand residue is the VERSION stamp: the file keeps 20260922230000 (already applied in the shared xaas_test DB) instead of the generator's 20260923074119 (2026-09-23) | UNSUPPORTED(generator-capability): ash_postgres 2.13.1 generate_migrations has no option to adopt an already-applied migration version (its timestamp is the wall clock), so a new version would re-add an existing column on every DB that ran the FRI-T2 file | ash_postgres upstream (adopt/rename applied migration) or ultracode-actuation-lease-pack (migration adoption step) | 2026-09-23

test/xaas/ultracode/recipe_worker_test.exs, test/xaas/ultracode/engine_test.exs (`provider allowlist` describe) | Chicago qualification of the recipe provider and allowlist (V23-T2R): real sandboxed Run/Epoch/Receipt rows, real git repos under a real containment root, real `mix format` through the resolved toolchain (node fallback and a real symlinked asdf layout), lost-lease falsifiers that make the REAL lease row stale between two real `sh` steps (expired via Epoch `:renew_lease` + re-claimed via `Lease.claim_next/3`; refused via `Lease.refuse/3`; expired under the undirected `Engine.fill/1`), the fence falsifier (a real git clean filter in the subject repo pauses the worker inside its `git add` after the last renew; the lease is re-claimed there and the ref update must refuse), the redaction falsifier (a step failing after its lease expired: the unsealable refusal returns and logs only the token fingerprint), unreadable `.tool-versions` falsifiers (a directory -> `:eisdir`, a dangling symlink -> `:enoent`), the canonical capabilityId source pin, and the malformed-allowlist config error (2026-09-23) | no admitted gate pack for recipe-provider/lease-loss qualification | ultracode-actuation-lease-pack gates/ | 2026-09-23

lib/xaas/ultracode/run.ex (`attribute :capability_id` with `max_length: 128` + `match:` the canonical capabilityId regex, and `:capability_id` in the `:create` accept list), lib/xaas/ultracode/semantic_work.ex (`@capability_id_source`, `capability_id_pattern/0` compiled `:dollar_endonly`, `optional_capability/1` in `admit/2`, the `"capability" => :capability` descriptor key, `capability_id:` in the Run attrs) | the work order's `sj:capabilityId` carried descriptor -> SemanticWork admission -> `Run.capability_id` (FRI-T2, repaired by V23-T2R, GC-26.9.23; PRD PR-009/PR-010, ARD §9-§10): the value space is the canonical pattern `^[a-z0-9][a-z0-9_.-]*:[a-z0-9][a-z0-9_.:-]*$` of the wave contract and of ggen_igniter semantic-jira-pack `shapes/work-order.shacl.ttl` (`sj:capabilityId` `sh:pattern`, friday/gc-fri-0800), pinned by recipe_worker_test's source-string test (2026-09-23) | UNSUPPORTED(generator-capability): the migration + snapshot ARE generated (row above), but no generator writes the resource attribute or the admission clause from the shape: ggen-marketplace xaas-public-ash-projection-pack projects only PUBLIC-ontology `sh:path`s (its gate refuses the private `sj:` vocabulary) into NEW `Xaas.Public.*` resources via `mix ash.gen.resource --attribute name:type[:required]` (no `sh:pattern`/`sh:maxLength` -> `constraints`), xaas-ash-core-pack renders governance changes/validations only, and `Ash.Resource.Igniter.add_new_attribute/4` (ash 3.x, deps/ash/lib/ash/resource/igniter.ex:392) inserts a caller-authored code block rather than deriving it from a shape; no ggen_igniter projection renders SemanticWork admission clauses from `work-order.shacl.ttl` | semantic-jira-pack (project `sj:capabilityId` `sh:pattern`/length into the Ash attribute constraint via ggen_igniter -> `Ash.Resource.Igniter.add_new_attribute/4`, and into the SemanticWork admission clause, then delete these hand lines) | 2026-09-23

lib/xaas/ultracode/verifier.ex (`contained_worktree/1`, `spawn_and_collect/6` made public with `@doc`/`@spec`; bodies unchanged) | the verifier's containment law and guarded process runner exposed as the ONE implementation the recipe provider reuses (FRI-T2, V23-T2R; ARD §10), instead of a copy in recipe_worker.ex (2026-09-23) | UNSUPPORTED(generator-capability): the runner itself is already a ledgered hand-written surface (the 2026-09-18 verifier.ex row below); visibility/API shape of a hand-written module has no generator | ultracode-actuation-lease-pack (verifier gate + recipe-provider shapes share one rendered runner) | 2026-09-23

scripts/sjira/fleet_matrix.py, scripts/sjira/test_fleet_matrix.py, docs/sjira/v26.9.23/courts/GC23-11.sh | GC23-11 bounded fleet (V23-F, GC-26.9.23; PRD §12 GC23-11, §10; ARD §4, §26 F6/F7): `universe` (union of fleet.ttl sj:ObservedRepository rows, survey.json perRepo and the ARD §4 headings of the accepted prose), `observe` (fetch --no-prune, HEAD, symbolic ref, default-branch SHA, ahead/behind, dirty count, open PRs; nothing else written to observed repos), `emit` (sorted matrix.ttl/matrix.md, byte-identical for identical inputs), `check-classification` (exactly one sj:FleetClassification per universe repo, required iff CriticalPath, Successor -> v23:GC-26.9.24, court-reference guard), `check-standing` (validator-ADMITTED ALIVE receipt at the exact int head per CriticalPath repo), the court script, and their Chicago qualification over real temp git repos (2026-09-23) | UNSUPPORTED(generator-capability): checked ggen-marketplace repo-as-found-pack (single-repo as-found survey templates), repo-reconciliation-pack (as-found/as-desired ledgers), github-controloutcome-observation-pack (run/job/PR observations) and ggen_igniter semantic-jira-pack on friday/gc-fri-0800 (no fleet terms, no fleet observer); the fleet observer `mix semantic_jira.observe --fleet` + runtime-templates/fleet.ttl.eex of GGEN_IGNITER-26922-15 exists only uncommitted in /Users/sac/wt/v26922/ggen_igniter/law-GGEN_IGNITER-26922-15-A, not on the critical-path base, so it is not admissible here; its vocabulary terms are reused verbatim in docs/sjira/v26.9.23/fleet/vocabulary.ttl | semantic-jira-pack (admit fleet vocabulary + classification shape; render the matrix from runtime-templates once GGEN_IGNITER-26922-15 lands, then retire emit/observe here) | 2026-09-23

scripts/sjira/prose_spans.py, scripts/sjira/test_prose_spans.py | v26.9.23 GC23-0 first-mile candidate binder (lane V23-X, PRD PR-002, ARD §5.2-5.3): `emit` binds each LLM-extracted quote to its unique UTF-8 byte span in the accepted prose and writes PVOCAB `sj:Proposition` Turtle (IRI = sha256 of sourceSha256:start:end:kind, candidateStanding UNKNOWN) with its own deterministic Turtle writer; `check` re-verifies digest, span bytes, IRI, kind, boundary class, gate coverage and (with --extract) byte-equal re-projection, and (with --summary) writes the verified tally a receipt copies its counts from (repair round 1: a hand-typed receipt count was refuted); plus its Chicago unittest qualification over real temp files and subprocesses (2026-09-23) | UNSUPPORTED(generator-capability): checked ggen crates (no prose/byte-span primitive), ggen_igniter lib/ and priv/ggen/semantic-jira-pack (no prose span or sj:Proposition vocabulary before this lane), ggen-marketplace semantic-documentation-contract-pack (sd:SourceSpan is line/column only, no byte offsets, source digest binding or quote lookup, and no generator renders a binder) and open-ontologies src/extract_scaffold.rs (schema->prompt scaffold + JSON conformance check, no source-span provenance): no generator renders a quote-to-byte-span binder or a deterministic PVOCAB serializer | semantic-jira-pack (PVOCAB proposition vocabulary + a prose-candidate emit/check template), or semantic-documentation-contract-pack extended with byte-offset spans | 2026-09-23

docs/sjira/v26.9.23/courts/GC23-0.sh, GC23-1.sh, GC23-2.sh, GC23-3.sh, docs/sjira/v26.9.23/courts/gi_mix.sh, docs/sjira/v26.9.23/courts/fixtures/f8/{discovered.md,extract-attack.json,extract-routed.json,successor-goal.ttl}, test/sjira/v26_9_23_goal_test.exs (V23-K tests) | the GC23-0..GC23-3 court bodies wiring the ggen_igniter first-mile and bootstrap machinery into xaas (lane V23-K, GC-26.9.23; PRD PR-001..PR-006, PR-017, ARD sections 5, 16, 17, 26 F1/F3/F8; path and exit contract from V23-P: 0 ALIVE, 75 UNKNOWN, else refused): GC23-0 = pinned prose sha + committed candidates/extraction/compiled + `prose_spans.py check --require-gates 13 --extract` + `mix semantic_jira.compile_prose --check` with the admitted count cross-checked against the verified tally; GC23-1 = V23-B's `scripts/sjira/bootstrap_court.sh` under courts/no_llm_env.sh over fleet/universe.json + goal.ttl + both checkouts' receipts/v26.9.23, plus its F3 exposed-key refusal; GC23-2 = two concurrent fresh compiles byte-identical to each other and to compiled/*.ttl, a shifted-span + smuggled-WorkOrder candidate mutation refused, an rdflib finiteness/placement law (order sj:checkpointOf = its proposition's sj:requiredBy, 1:1 with required Postcondition/Invariant/Falsifier propositions) and F8 (discovered prose refused against GC-26.9.23 by the root's sj:sourceSha256 pin, admitted under the successor fixture root v23:GC-26.9.24 and sj:checkpointOf+ it only); GC23-3 = `compile_prose --admit-goal` over goal.ttl (context: the Friday goal) and compiled/orders.ttl (context: goal.ttl) with rdflib count cross-checks, and F1 by one batched graph per class (control copy, 11 single-field-deleted copies refused naming the field, no-exclusion copy admitted); gi_mix.sh = `mix` in GGEN_IGNITER_DIR under the no-LLM env against a private APFS clone of its `_build/$MIX_ENV` (MIX_BUILD_PATH, removed on exit; the judged checkout is never written -- wave B1c); the F8 fixture prose/extractions/successor root; tests for the compiled projection's input binding + placement law, the F8 fixture, the 75 mapping, GC23-0/GC23-3 mutation refusals and gi_mix.sh's read-only build over a real tmp mix project (2026-09-23; B1c 2026-09-23). compiled/{propositions,orders}.ttl are GENERATED by compile_prose (not residue); the candidates needed 0 extraction corrections | UNSUPPORTED(generator-capability): as for the GC23-4..8 row: no pack renders a court body from a gate's sj:acceptance/sj:falsifier (checked the 326 ggen-marketplace packs: 0 files name sj:courtCommand, compile_prose, admit-goal or bootstrap_court; gate-vacuity-court-pack and semantic-gate-witness-court-pack render Python witness courts over gate outputs, not shell courts over mix tasks) and ggen_igniter semantic-jira-pack has no court-body template | semantic-jira-pack (court-body projection from sj:acceptance/sj:falsifier + a witness map; the F8 successor fixture becomes the GC23-12 successor goal once V23-H lands it) | 2026-09-23

lib/xaas/sa2a/bridge.ex | `Xaas.Sa2a.Bridge` GenServer: `Port.open/2` process wrapper (`{:line, ...}` framing to match autofde-lab's newline-delimited JSON-lines stdio protocol) + `validate/1`, `admit/2`, `plan/2`, `execute/2`, `replay/2` public functions calling it (2026-09-20) | no admitted pack renders a Port GenServer wrapper or its JSON-lines encode/decode; `sa2a-bridge-pack`'s ontology names this module by string (`s2b:fromPlane "Xaas.Sa2a.Bridge.execute"` etc.) without generating it, the same construct-only boundary `xaas-castle-bridge-pack` observes for `Xaas.Castle.Reactor` | ggen-marketplace sa2a-bridge-pack (admit from this proven shape) | 2026-09-20

lib/xaas/sa2a/route.ex, scripts/gen_sa2a_route_fixture.exs (tuple package projection) | `Xaas.Sa2a.Route` G5 SA2A conservation: per-representation tuple normalization (sJira wo.json row / SA2A task route-tuple data part / SemanticWork epoch bridge), the contract tuple digest, `:ultracode_construction_recipes` capability resolution, and `conserve/3` refusing `admission_vacuous` per field; the generator's camelCase tuple package handed to the real `GgenIgniter.SemanticA2A.task_from_work_order/2` (2026-09-22) | no generator renders a per-representation field map or a canonical-JSON tuple digest from the sj: vocabulary contract, and `task_from_work_order/2` has no tuple projection of its own: UNSUPPORTED(generator-capability) | ggen-marketplace sa2a-bridge-pack (route-conservation extension from the sj: tuple fields) + ggen_igniter SemanticA2A tuple projection | 2026-09-22

lib/xaas/ultracode/lease.ex | ActuationLease kernel over Run/Epoch/Receipt, incl. the Receipt `:for_epoch` lawful read carve-out (2026-09-17), + `actuate/2`: a live lease's admitted, per-provider-registry-gated reach into `Xaas.Actuation.run/4` (2026-09-17) | no admitted pack expresses a lease/claim/admit/close edge on the Ultracode seam, nor its registry-gated bridge onto the separate Path-A actuation kernel | ggen-marketplace ultracode-actuation-lease-pack (admit from this proven shape) | 2026-09-17

lib/xaas_web/controllers/execution_fabric_controller.ex | MCP JSON-RPC + hook HTTP transport + sealed-receipt read route (GET /internal-api/execution/epochs/:epoch_id/receipts) + atom-safe refuse-reason mapping + `actuate` MCP tool (ZCode-UI-as-actuator seam, forwards to `Lease.actuate/2`) (2026-09-17) | no admitted pack renders a stateless MCP server controller inside Phoenix with internal-token gate | ggen-ecosystem-mcp-surface-pack family extension | 2026-09-17

lib/mix/tasks/xaas.receipts.ex | operator sealed-receipt inspection task over the authorized `:for_epoch` path | no admitted pack renders an operator receipt-read task on the Run/Epoch/Receipt seam | ultracode-actuation-lease-pack | 2026-09-17

priv/zcode_plugin/packs/zcode-plugin-pack/templates/{command-xaas,agent-xaas-worker,skill-xaas-worker}.md.tmpl | worker doctrine prose bodies (frontmatter is projected from ontology.ttl) | irreducible prose: no pack models doctrine text as ontology fragments | zcode-plugin-pack doctrine fragments | 2026-09-15

scripts/xaas-glm-failover-dispatcher.sh, scripts/install-glm-failover-launchd.sh | unattended failover dispatcher (poll, drain, reaper, timeout, stall alert) and its launchd installer (2026-09-18) | no admitted pack renders a provider-failover dispatcher or a launchd unit from the Run/Epoch seam | ultracode-actuation-lease-pack | 2026-09-18

test/xaas/zcode_plugin/*.exs | gate, lease-script, doctrine and projection-drift qualification tests | no admitted gate pack for plugin-projection qualification | zcode-plugin-pack gates/ | 2026-09-18

lib/xaas/ultracode/verifier.ex | fabric-executed verifier suite runner: name-only registry lookup, worktree containment root, env allowlist through `env -i`, process-group kill, bounded output, clean-tree and head discipline (2026-09-18) | no admitted pack renders a fixed-argv suite runner with these fences from the Run/Epoch seam | ultracode-actuation-lease-pack (verifier gate) | 2026-09-18

lib/xaas/ultracode/autonomic.ex, lib/mix/tasks/xaas.autonomic.run.ex | autonomic loop controller: deterministic backlog sensing, per-item worktree/ticket/Run/Epoch planning, top-up pacing that halves on provider rate refusal, fabric-verified repair loop, serialized --no-ff promotion, canonical verification at the integration head, ndjson ledger and terminal receipt (2026-09-18) | no admitted pack renders a closed-loop controller over the Run/Epoch/Receipt seam; nearest prior art is repository-factory-scheduler-pack (PLAN_ONLY) and autofde-lab autonomic-loop doctrine (stage 4 UNKNOWN) | ultracode-actuation-lease-pack + a scheduler pack promoted from this shape | 2026-09-18

lib/mix/tasks/xaas.autonomic.controls.ex | live negative-control harness: crafted bad candidates closed over the real HTTP MCP surface, sealed receipts compared to expected typed gate failures (2026-09-18) | no admitted pack renders falsifier fixtures for a definition-of-done court; nearest prior art is gate-vacuity-court-pack's executable falsifier template | a `chicago-dod-court-pack` (falsifier fixtures generated from the gate catalogue) | 2026-09-18

lib/xaas/ultracode/worktrees.ex | operator-side worktree provisioning from a registered repo alias + base_sha under a containment root | no admitted pack renders worktree provisioning for the Epoch seam | ultracode-actuation-lease-pack | 2026-09-18

priv/verifiers/aps_dod_court.py, priv/verifiers/aps_backlog.py | APS Chicago-TDD definition-of-done court (exact-head, scope, mock, assertion, canonical gates, mutation kill) and deterministic backlog derivation; receipts conform to APS evidence-receipt.schema.json | no admitted pack renders a Python-repo DoD court or mutant catalogue; nearest prior art is autofde-lab `run_chicago_qualification.py` and gate-vacuity-court-pack | a `chicago-dod-court-pack` lifted from autofde-lab CHI-* gates | 2026-09-18

priv/verifiers/tests/*.py, test/xaas/ultracode/{verifier,lease_verifier,worktrees}_test.exs | qualification of the verifier, close integration, provisioning, court and backlog | no admitted gate pack for verifier qualification | ultracode-actuation-lease-pack gates/ | 2026-09-18

test/xaas/ultracode/lease_test.exs | lease-edge qualification tests, + `actuate/2` registry-admission/refusal/authority-evidence tests (2026-09-17) | no admitted gate pack for lease semantics | ultracode-actuation-lease-pack gates/ | 2026-09-15

lib/xaas/ultracode/semantic_receipt.ex, lib/xaas/ultracode/semantic_receipt/aps_dod.ex, lib/mix/tasks/xaas.semantic.receipt.ex | sealed-receipt export in the canonical-graph reconciler contract (canonical-JSON digest identical to the graph side's `digest_exact/1`, `bridge` echoed verbatim) and the `aps-dod` court-gate adapter that maps observed gates onto acceptance/falsifier results (2026-09-20) | no admitted pack renders the fabric-to-graph receipt projection or a court-gate-to-criterion mapping | ultracode-actuation-lease-pack | 2026-09-20

lib/xaas/receipt/r_projection.ex, lib/xaas/ultracode/semantic_receipt.ex (`sealed/1`), test/xaas/receipt/r_projection_test.exs | `Xaas.Receipt.RProjection`: XaaS semantic export -> fleet R schema, admitted only when fabric-sealed (digest present, recomputes, and equals the fabric's own re-export via `SemanticReceipt.sealed/1`; replay exits read from the sealed closing evidence, never derived from a status) (`~/.claude/dfcm/receipt.schema.json`: identity/authority/consequence/replay/standing, typed broken_term) written as `<stem>.r.json`, qualified against the real `validate_receipt.py` (2026-09-22, GC-FRI-0800 G7, lane FRI-T3) | UNSUPPORTED(generator-capability): no admitted pack renders a native-receipt -> R-schema projection; receipt-provenance-unification-pack models autofde-lab receipt contracts and generates a Python validator, not an Elixir projection onto the dfcm schema | ultracode-actuation-lease-pack (receipt projection) or receipt-provenance-unification-pack extended with the dfcm R contract | 2026-09-22

lib/xaas/ultracode/court_receipt.ex (`exit_status` result format), lib/xaas/ultracode/target_suites.ex (`ggen-igniter-format` suite, `elixir_env/3`, `elixir_format_suite/3`), lib/xaas/ultracode/semantic_receipt.ex (produced-court-receipt passthrough for adapterless suites), test/xaas/ultracode/{court_receipt,semantic_receipt}_test.exs | exit-status court verdicts, the Friday reference-class format court, and the binding passthrough that lets promotion read non-aps court results (2026-09-22, GC-FRI-0800 G7, lane FRI-T3) | UNSUPPORTED(generator-capability): same gap as the `target_suites.ex` `sj_program_suites/0` row in the Shrunk section below -- no admitted pack renders a verifier-suite declaration or a court-verdict format from an ontology fact | ultracode-actuation-lease-pack (verifier gate) + a target-suite pack promoted from this shape | 2026-09-22

lib/xaas/ultracode/semantic_crown.ex, lib/mix/tasks/xaas.semantic.{materialize,crown}.ex | autonomics crown controller: observation, SHACL-admitted candidate, frontier, descriptor, Run/Epoch, worker, sealed receipt, ledger transition, dependent unlock, fresh-process replay and a live negative control, driving the graph side as real `mix semantic_jira.*` processes (2026-09-20) | no admitted pack renders a cross-system loop controller | ultracode-actuation-lease-pack | 2026-09-20

priv/repo/migrations/20260920230000_add_semantic_bridge_to_ultracode_runs.exs, lib/xaas/ultracode/run.ex (`semantic_bridge`) | additive nullable jsonb column storing the descriptor bridge opaquely; hand-written like the sibling semantic-identity migration (no resource snapshot) (2026-09-20) | ash.codegen snapshot for the semantic-identity/bridge columns not generated (shrunk 2026-09-23, V23-T2R: the court_map + semantic_bridge snapshot is now generated, priv/resource_snapshots/repo/ultracode_runs/20260923074101.json; the migration bodies stay hand-written) | ultracode-actuation-lease-pack | 2026-09-20

lib/xaas/ultracode/semantic_work/admission_binding.ex, lib/xaas/ultracode/semantic_work.ex (`admit/2` `binding:`), lib/mix/tasks/xaas.semantic.materialize.ex (`--binding`) | XaaS-side recomputation of the semantic work-order snapshot digest and fail-closed graph_digest binding (anchors: `admitted_work_order` snapshot, `bridge.source_snapshot_digest`, `admission_digest` envelope; modes auto/snapshot/graph) closing SJ-001 falsifier 1; the canonical digest form is mirrored by hand from `GgenIgniter.SemanticJira.digest/1` via `SemanticReceipt.digest/1` (2026-09-21) | xaas depends on ggen_igniter 26.9.12 from hex, which has no `SemanticJira`, so the admitted digest cannot be recomputed by the graph library itself; no admitted pack renders a digest-binding verifier for a graph-emitted execution descriptor; the real `semantic_jira.descriptor` emitter also sends no `admitted_work_order` yet (UNSUPPORTED, graph-side) | semantic-jira-pack (emit `admitted_work_order` and render this verifier from the descriptor ontology, then delete this module) | 2026-09-21

test/xaas/ultracode/{semantic_receipt,semantic_crown}_test.exs | Chicago qualification of the exporter, court adapter, materialize task and the end-to-end crown with a refused-candidate control (2026-09-20) | no admitted gate pack for the fabric-to-graph loop | ultracode-actuation-lease-pack gates/ | 2026-09-20

lib/xaas/coupling/coupling_run.ex, lib/xaas/ledger/event_log.ex, lib/xaas/operations/autofde_planner_{cache_hotset,cache_stats,candidate,catalog,match}.ex | one-line `use Xaas.Resource` adoption on seven owned Ash resources (SJ-004; supersedes the registry-test `@pending` exemption for them) | no generator adopts a base resource on existing Ash modules: UNSUPPORTED(generator-capability) | ash-resource / ontology-projection pack (ggen_igniter Ash resource generator) | 2026-09-21

lib/xaas/ultracode/zcode_package.ex | `Xaas.Ultracode.ZcodePackage`: reads ~/dev/zcode-cli/package.json (name, bin.zcode contained on REAL paths via a symlink-resolving walk, engines.node floor) and measures the worker node via a deadline-bounded, process-group-reaped `--version` probe | no admitted pack expresses an external-CLI package contract reader | zcode-plugin-pack | 2026-09-21

lib/xaas/ultracode/provider_health.ex | `Xaas.Ultracode.ProviderHealth`: one-call zcode provider health (`check/1` -> `{:ok, %{zcode_version, zcode_bin, node_version}}` or a typed error, `gate/1` -> `:ok | {:error, {:provider_unhealthy, reason}}`) over `ZcodePackage.verify_node/3` (`:node_version_timeout_ms`, default 5000, so the gate can never hang), for the autonomic loop to gate leases on | no admitted pack expresses an external-CLI provider health probe | zcode-plugin-pack | 2026-09-21

lib/xaas/ultracode/process_group.ex | `Xaas.Ultracode.ProcessGroup`: verified group-wide TERM/poll/KILL/poll kill, extracted from `Dispatch` so `ZcodePackage`'s `--version` probe reaps a hung shim with the same mechanics (Dispatch delegates to it; `Verifier` still carries its own copy) | no admitted pack expresses OS process-group lifecycle | ultracode-actuation-lease-pack | 2026-09-21

lib/mix/tasks/xaas.stop_court.ex, test/mix/tasks/xaas_stop_court_test.exs | `mix xaas.stop_court` (FRI-T5, GC-FRI-0800): reads a `sj:GoalCheckpoint` goal graph (docs/sjira/v26.9.22/friday/goal.ttl), runs each gate's `sj:courtCommand` in its own process group under a deadline, writes one R-schema gate receipt per gate, projects validator-ADMITTED receipts (gate subject / WorkOrder tuple digest bound) into `sj:Receipt` nodes and evaluates the root's `sj:stopQuery` (stop.rq) through the dep-tree oxigraph NIF as its SELECT-existence equivalent (the NIF answers SELECT only, no ASK form), plus its Chicago qualification (2026-09-22) | UNSUPPORTED(generator-capability): no admitted pack renders a stop-court runner from the GoalCheckpoint vocabulary, and `GgenIgniter.Native.GraphNif` has no ASK/boolean result form, so the ASK -> SELECT * LIMIT 1 adapter is hand-written | semantic-jira-pack (render the stop court from sj:GoalCheckpoint/sj:courtCommand/sj:stopQuery; add a boolean ASK result to ggen_graph_nif, then delete the adapter) | 2026-09-22

docs/sjira/v26.9.23/courts/{GC23-0..GC23-12,check_scripts}.sh, lib/mix/tasks/xaas.stop_court.ex (checkpoint registry, court env, exit-code outcome, transitive orders), test/sjira/v26_9_23_goal_test.exs | GC-26.9.23 governing-object residue (V23-P, PRD section 12/13, ARD section 19): 13 gate court stubs that print `UNKNOWN: <gate> machinery lands in lane <LANE>` and exit 75 (GC23-0 also pins the accepted-prose sha256), the court-script contract check, the stop court's per-checkpoint registry (GC-FRI-0800, GC-26.9.23), XAAS_DIR/GGEN_IGNITER_DIR court env, 0/75/124/other outcome mapping and `sj:checkpointOf+` order closure, plus the goal graph's Chicago qualification (2026-09-22). goal.ttl, stop.rq and prd-ard.md are canonical sources, not residue | UNSUPPORTED(generator-capability): no admitted pack renders gate court scripts, a court-contract check or a checkpoint registry from sj:GoalCheckpoint/sj:courtCommand (checked ggen-marketplace gate-vacuity-court-pack and semantic-gate-witness-court-pack: both render Python falsifier/witness courts, neither reads sj:courtCommand) | semantic-jira-pack (render court stubs and the registry from the goal graph; same paydown as the FRI-T5 row) | 2026-09-22

lib/mix/tasks/xaas.stop_court.ex (order-receipt dirs), test/mix/tasks/xaas_stop_court_test.exs, test/sjira/v26_9_23_goal_test.exs | stop-court order receipts from both critical-path repos (lane V23-L, PRD PR-017, ARD §12, §19): the checkpoint registry names an ordered `order_receipts_dirs` list (GC-26.9.23: xaas `receipts/v26.9.23`, then `$GGEN_IGNITER_DIR/receipts/v26.9.23` resolved with the court-env precedence --ggen-igniter-dir > GGEN_IGNITER_DIR > registry default; GC-FRI-0800 unchanged: one dir), repeatable overriding `--order-receipts-dir`, per-order lookup across the dirs (same bytes = one receipt judged from the first dir; different bytes = typed `REFUSED(duplicate_order_receipt)`, broken term R_missing_identity, never a silent pick), the STOP receipt's `stop` section (per dir: a typed repository probe -- git toplevel + HEAD, SHA-1 or SHA-256, else `repo_head_why` = directory absent / no git checkout (git finds no repository and no ancestor holds `.git`) / unborn HEAD / the git failure, never a git failure recorded as no checkout; per order: dir, repo HEAD, committed-at-HEAD blob check typed true / false + reason / null + git failure, found_in, refusal), and `sj:exclusion` 0..n in the tuple (absent = []), plus their Chicago qualification (2026-09-23) | UNSUPPORTED(generator-capability): same gap as the FRI-T5 and V23-P rows -- no admitted pack renders a stop-court runner or its checkpoint registry from sj:GoalCheckpoint; the order-receipt directory list is registry data, not a projection of goal.ttl (goal.ttl names no receipt location, and may not assert receipts) | semantic-jira-pack (render the stop court and its registry, including per-repo order-receipt locations, from the goal graph + a repo map; same paydown as the FRI-T5 row) | 2026-09-23

lib/mix/tasks/xaas.stop_court.ex (release evidence), test/mix/tasks/xaas_stop_court_test.exs, test/sjira/v26_9_23_goal_test.exs | stop-court release evidence (lane V23-S; operator release sequence steps 3 and 5, ARD §12, §27): every gate and STOP receipt carries `identity.repo` as the GitHub slug from the first github.com remote (worktree path in `identity.worktree`, `repo_source` naming the remote, never its URL), `verifier` (runner + compile-time sha256 of its own source + at-subject blob check, court command + sha256, court script + sha256 read before it runs + typed at-subject blob check true / false + reason / null + git failure, validator, schema and stop-query sha256), `toolchain` (Elixir, OTP release, ERTS, python3 version and path) and `replay.commands[].invocation` (argv with every explicit option resolved, `--only <gate>`, env MIX_ENV / GGEN_IGNITER_DIR / GC23_FLEET_RECEIPTS_DIR + caller GC23_*/DFCM_*, cwd, shell line with `env -u` for unset); the STOP receipt derives the ARD §27 counters (REQUIRED_UNKNOWN from this run's judged gates and non-Successor orders; UNCLASSIFIED_REQUIRED_WORK by re-executing the GC23-11 court's check-classification argv from the checkpoint registry; LLM_INVOCATIONS_ON_KNOWN_REFERENCE_PATH / REQUIRED_LLM_KNOWN from episodes fmt-1 and me-2 OCEL by the GC23-5 + GC23-9 court rules; UNRECEIPTED_ACTUATION from every episode ledger bound through OCEL StandingChanged -> ReceiptSealed), unreadable sources null with the reason, plus their Chicago qualification (2026-09-23) | UNSUPPORTED(generator-capability): checked ggen-marketplace receipt-provenance-unification-pack (renders a receipt validator + contract census, not a receipt producer with verifier/toolchain identity) and chatman-ecosystem-release-pack (release composition law SPARQL gates; release-instance facts stay consumer-owned); no pack renders a stop-court runner (same gap as the FRI-T5, V23-P and V23-L rows), and the GC23-11 check-classification argv is registry data duplicated from docs/sjira/v26.9.23/courts/GC23-11.sh (the counter records that script's sha256) | semantic-jira-pack (render the stop court, its registry and the per-gate court argv from the goal graph so the counter and GC23-11 share one declared step); chatman-ecosystem-release-pack (the ARD §27 counter set as release law over the STOP receipt) | 2026-09-23

## Paydown plan

1. Qualify the rendered plugin against ZCode 3.11.2 live (hook event names,
   plugin-root expansion variable, .mcp.json env-header support).
   Partial as of 2026-09-16: MCP connectivity confirmed ALIVE end-to-end
   against a live ZCode 3.11.2-25 CLI session, via an `xaas-execution` MCP
   server registered directly in ZCode's `config.json` `mcp.servers` key.
   The plugin's own `zcode plugins install` path is still BLOCKED — not by
   an xaas-side defect, but by a confirmed ZCode 3.11.2-25 bug: install
   hardcodes an empty env-var-substitution context, so a plugin's
   bearer-token secret can never resolve at install time regardless of the
   process environment. Remaining qualification (hook event names,
   plugin-root expansion variable, .mcp.json env-header support) still
   needs the install path and is blocked pending a ZCode-side fix.
   Progress 2026-09-17 (2f49261): `.mcp.json.eex` + `plugin.json.eex`
   now source the MCP bearer token from ZCode `user_config` instead of
   the session environment, removing the xaas-side dependency on the
   broken env-var-substitution path; live install-path re-qualification
   still pending.
2. Promote `priv/zcode_plugin/` (ontology + templates) into
   `ggen-marketplace` as `zcode-plugin-pack` once qualified. DONE
   2026-09-18 (locally): the repo-local `xaas.gen_zcode_plugin` generator is
   deleted; `cd priv/zcode_plugin && ggen sync run` renders the plugin and
   marketplace manifests, hook config and frontmatter from `ontology.ttl`
   into the tracked `marketplace/` projection (drift-checked by
   `test/xaas/zcode_plugin/projection_test.exs`). Remaining: publish to the
   marketplace, and lift the gate's allowlist constants into the ontology.
   Progress 2026-09-21 (SJ-003): the plugin/marketplace/MCP/hook manifests,
   frontmatter and the PreToolUse gate script now render from a repo-local
   path pack, `priv/zcode_plugin/packs/zcode-plugin-pack` (bound in
   `priv/zcode_plugin/ggen.toml`, pinned by `ggen.lock`); the gate's
   allowlists are `zp:AllowedTool` / `zp:ToolMapping` / `zp:AllowedBashVerb` /
   `zp:ForbiddenGitFlag` / `zp:DeniedPath` individuals in `ontology.ttl`.
   Remaining: publish the pack to ggen-marketplace (this order's path_scope
   excludes that repo), doctrine-prose fragments, plugin-projection gates.
3. Extract the lease kernel's invariants into `ultracode-actuation-lease-pack`
   (SHACL + Ash render targets), including the bulk_update
   extracted-filter guard as a gate, and (2026-09-17) the `actuate/2`
   registry-lookup + forced-authority-evidence shape as a second gate,
   distinct from the admit_tool construction/consequence fence gate.
   Progress 2026-09-21 (SJ-003): the client-side lease state-file protocol
   (`xaas-lease.mjs`) now renders from `ul:LeaseStateProtocol` facts via the
   repo-local pack `priv/zcode_plugin/packs/ultracode-actuation-lease-pack`;
   its `ul:stateDirName` is the same fact the gate reads. The server-side
   kernel (`lease.ex`, controller, verifier, autonomic, worktrees, receipts,
   crown) is NOT rendered and its rows stay Active.
4. beam4pm integration is an ontology fact, not code: execution-provider
   observation + PlannerLease≠ActuationLease axiom in beam4pm's ontology.

## Shrunk

Format: `path | what shrunk | date`

priv/templates/zcode_plugin/hooks/post_tool_use.mjs.eex | fixed nested `event: {...}` body wrapper that didn't match the flat body execution_fabric_controller expects — hook now posts tool/tool_use_id/cwd/session_id directly (committed a11bf7a) | 2026-09-16
priv/templates/zcode_plugin/hooks/post_tool_failure.mjs.eex | fixed the same nested-vs-flat body mismatch, plus a wrong hook URL segment (was posting to `post_tool_use`, now posts to `post_tool_use_failure`) (committed a11bf7a) | 2026-09-16
priv/templates/zcode_plugin/hooks/user_prompt_submit.mjs.eex | fixed the same nested-vs-flat body mismatch (committed a11bf7a) | 2026-09-16
priv/templates/zcode_plugin/commands/xaas.md.eex | fixed `close_candidate` key name mismatch — doc told workers to send `standing`, the controller/lease contract expects `outcome` (committed a11bf7a) | 2026-09-16
priv/templates/zcode_plugin/.mcp.json.eex + .zcode-plugin/plugin.json.eex | MCP bearer token now sourced from ZCode `user_config`, not the session env — paydown item 1's install-path blocker no longer has an xaas-side dependency (committed 2f49261) | 2026-09-17

priv/zcode_plugin/templates/script-xaas-gate.mjs.tmpl | row removed: template promoted into `packs/zcode-plugin-pack`; the tool sets, tool map, git subcommand/flag allowlists, read helpers and denied home paths are ontology individuals, and the script is rendered from them (re-sync is byte-identical to the previous hand-written projection; mutating an individual changes the rendered script). The enforcement logic itself remains hand-authored inside the pack template (pack capital, not consumer residue) | 2026-09-21
priv/zcode_plugin/templates/script-xaas-lease.mjs.tmpl | row removed: template promoted into `packs/ultracode-actuation-lease-pack`, rendered from `ul:LeaseStateProtocol` (state dir, script name, install path, conflict/usage exit codes); the gate reads the same state-dir fact | 2026-09-21

Wave-4 paydown direction (2026-09-17): the zcode-plugin-pack rows shrank
toward pack admission (templates now contract-clean + credential-correct;
remaining blocker is the ZCode-side install bug, not xaas code). The
mcp-surface controller row and the lease row grew (+atom-safe refuse
reason, +sealed-receipt read path on controller/receipt/mix-task) —
disclosed growth; owner packs unchanged; both new edges are
contract-compatible extensions of the shapes those packs admit from.

Wave-5 addition (2026-09-17): "make the ZCode UI also work as the
actuator." `Xaas.Ultracode.Lease.actuate/2` + the `actuate` MCP tool give a
live-leased provider worker a second, narrower, admitted surface onto
`Xaas.Actuation.run/4` (Path A) — the same kernel
`Xaas.Marketplace.Changes.ApplyProviderStatusChange` already calls, not a
new DO path. This is explicitly NOT the "AuthorityCeiling on admit_tool"
widening the prior wave's authority-mapping receipt (`wave1-06-authority.md`)
warned against: `admit_tool/2`'s Bash/git_push/publish fence is untouched in
substance and still independently tested on the same lease token an
`actuate` call used. The new surface is gated by (a) an explicit, opt-in,
per-provider `{resource, action, subject}` registry
(`config :xaas, :ultracode_actuation_registry`, empty by default —
fail-closed, same convention as `:ultracode_provider_tools`), (b) Path A's
own unmodified admission court (`FrontierEvidence`/`CausalAdmission`), and
(c) forced, lease-provenanced, non-empty authority evidence
(`authorize?: false` always; the caller cannot supply an empty authority
map or `authorize?: true`). No production registry entries are configured
anywhere in this repo today — the surface is code-complete and tested but
inert until an operator opts a specific provider into a specific
already-Reactor-admitted action.

Adversarial-review correction, same wave (2026-09-17): a 3-lens review
(security/correctness/consistency, each finding independently re-verified
by a skeptical second pass with real reproduction, not inspection alone)
found and this pass fixed 3 real defects before landing:

1. **CRITICAL, introduced by this addition**: `actuate/2` originally
   accepted `subject_id` as raw wire input with `authorize?: false` and an
   authority map carrying no subject/org scope — any live lease of a
   registered provider could name ANY row of the registered resource, not
   just one tied to its own grant (a broken-object-level-authorization gap,
   demonstrated live by the review against this repo's own passing test).
   Fixed by removing `subject_id` from the wire contract entirely: the
   registry's third tuple element (`:no_subject` or a fixed, operator-named
   subject_id string) is now the ONLY source of the acted-on subject —
   never the caller. A regression test proves a wire-supplied `subject_id`
   is ignored and cannot redirect the actuation.
2. **MAJOR, PRE-EXISTING (not introduced this wave)**: `admit_tool/2`'s
   `cond` checked the operator-configurable `admitted_tools/1` before
   `@refused_consequence_tools`, so a misconfigured `:ultracode_provider_tools`
   entry naming "Bash"/"git_push"/"publish" could have won an earlier
   clause and silently defeated the fence — contradicting this file's own
   "the ceiling is a fence, not a configurable grant" claim, dormant today
   (no such config exists anywhere in this repo) but untested until this
   review. Fixed by reordering the `cond` so the hardcoded refusal always
   wins regardless of config; a regression test proves it. Not this wave's
   own new code, but fixed here because this wave's own moduledoc addition
   makes explicit claims about that fence's robustness.
3. **MAJOR**: the `actuate` MCP dispatch clause pattern-matched any JSON
   type for `lease_token`, so a non-string value raised an unhandled
   `FunctionClauseError` through the controller instead of a typed refusal.
   Fixed with an `is_binary` guard + fallback clause; a regression test
   proves the typed error, not a crash.

Full adversarial-review transcript: workflow run `wf_c87d7295-9a9`
(7 agents, 3 review lenses + 4 independent verify passes, all 4 raw
findings confirmed real on re-verification, all fixed before this ledger
entry was written).

lib/xaas/ultracode/target_suites.ex (`sj_program_suites/0` and helpers) | fabric verifier suites for the four Semantic Jira program targets (xaas, autofde-lab, gymact, ggen-igniter): pinned toolchain env, seed take/publish and per-run-DB `mix test` shell wrappers, Python subject-identity step, curated exclusions of tests that rewrite tracked files (2026-09-21) | no admitted pack renders a verifier-suite declaration (argv, env allowlist, timeouts, seed/partition wrappers) from a target-repo ontology fact | ultracode-actuation-lease-pack (verifier gate) + a target-suite pack promoted from this shape | 2026-09-21

lib/xaas/ultracode/repos.ex (`refresh/1`, `refresh` entry field), lib/mix/tasks/xaas.ultracode.repos.ex (`--refresh`, `--refresh-before-sense`), lib/xaas/ultracode/sensing.ex (`profile_for/1`), lib/xaas/ultracode/autonomic.ex (`derive_items/3`, refresh-before-base_sha) | non-destructive fetch + `merge --ff-only` clone refresh and the registry-name -> declared-sensing-profile binding that lets the autonomic loop sense registered repos from their own tickets (2026-09-21) | no admitted pack renders a registry-entry field, a clone-refresh state machine, or a name-to-profile resolver on the Ultracode seam | ultracode-actuation-lease-pack + a scheduler pack promoted from this shape | 2026-09-21

test/xaas/ultracode/{repos_refresh,autonomic_profile_sense,sj_program_registry}_test.exs | Chicago-style qualification of clone refresh, profile-driven sensing through `Autonomic.sense/1`, the committed dev.exs baseline for the four targets, the suite environment law and the Python subject-identity guard (2026-09-21) | no admitted gate pack for registry/suite qualification | ultracode-actuation-lease-pack gates/ | 2026-09-21

(baseline established 2026-09-15; note: the first attempt at this change
invented parallel ExecutionWorker/WorkContract/Execution resources and was
reverted — `backup/execution-fabric-v1` — in favor of extending the
existing Ultracode seam)
