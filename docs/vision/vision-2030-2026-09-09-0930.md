# Vision 2030 — Cycle 2026-09-09-0930

## Charter: fix the real mock-grep false-positive gap in `mix xaas.verify_and_commit`

**Real, grounded finding** (disclosed honestly by cycle 0830, re-confirmed real just
now by reading `lib/mix/tasks/xaas.verify_and_commit.ex:132-145` directly): the
task's `mock_grep_stage/0` runs the pattern
`"unittest\\.mock|Mock\\(|MagicMock|patch\\(|monkeypatch|Mox\\b|:meck|meck\\."`
over the WHOLE `test/` + `lib/` trees unscoped, and self-matches on things that are
not real mocking-library usage:

- `Phoenix.ConnTest`'s real `patch/3` HTTP PATCH helper, used in ~40 real controller
  test files (`|> patch("/api/...")`) — a real HTTP verb call, not a mock.
- Ash's own `patch(:update)` DSL call inside `Xaas.Operations.Incident`.
- The task's own literal pattern string at `lib/mix/tasks/xaas.verify_and_commit.ex:140`
  (grepping for "patch(" necessarily matches the source line that defines the
  pattern "patch\\(").
- The literal word "monkeypatch" inside its own test fixture
  (`test/mix/tasks/xaas_verify_and_commit_test.exs`), which deliberately writes
  that word to prove the stage's own detection works.

**Real consequence**: `mix xaas.verify_and_commit` cannot currently reach its commit
stage on THIS repo's actual tree — every real invocation this session has halted at
mock-grep, never legitimately (no real mocking-library usage has ever been found),
purely on these 4 false-positive classes. This makes the tool currently unusable for
its stated purpose (a Verify stage future cycles can shell out to instead of an LLM
re-deriving the sequence) — a real, load-bearing defect, not cosmetic.

## This cycle's concrete charter

1. **Real fix**: narrow the mock-grep pattern so it still catches real mocking-library
   usage (`unittest.mock`, `Mock(`, `MagicMock`, `monkeypatch`, `Mox\b`, `:meck`,
   `meck\.`) but stops matching `patch(` as a bare substring. Real options to
   evaluate: (a) require `patch(` to be preceded by specific mocking-library
   qualifiers only (`mock.patch(`, `unittest.mock.patch(`) rather than any `patch(`,
   since this is an Elixir repo and `patch(` is legitimately used by both
   `Phoenix.ConnTest` and Ash's own DSL — the current pattern looks copy-pasted from
   a Python-repo convention (`unittest.mock.patch`) without being adapted for an
   Elixir codebase's real vocabulary; or (b) drop the bare `patch\(` alternative
   entirely for this repo (Python's `patch(` isn't a realistic false-negative risk in
   an Elixir codebase with no Python test files) and rely on `Mox\b`/`:meck`/`meck\.`
   which are Elixir-specific and unambiguous. Choose via real adversarial verify, not
   the first idea.
2. **Real self-test regression proof**: the task's OWN test file
   (`test/mix/tasks/xaas_verify_and_commit_test.exs`) has a fixture asserting
   "monkeypatch" IS detected — confirm the fix does not break that real detection
   while also confirming it no longer false-positives on `patch(`. Add a new real
   Chicago-style test proving a file containing legitimate `Phoenix.ConnTest`
   `patch/3` calls or Ash `patch(:update)` DSL is NOT flagged.
3. **Real FMEA/RCA sweep (loop-until-dry)**: search for any OTHER pattern in the
   mock-grep regex that risks the same class of false positive against this specific
   Elixir/Phoenix/Ash codebase's real vocabulary (e.g. does `Mock(` collide with any
   real domain name in this repo? Does `:meck` collide with any real atom?).
4. **Real dogfood re-run**: after the fix, run `mix xaas.verify_and_commit
   --message-file <this cycle's message>` against this repo's live tree and report
   whether it NOW reaches the commit stage for real, or what (if anything) still
   blocks it.
5. **5+ real Chicago-style MCP/A2A persona avatar tests** — since this cycle's
   changes only touch a Mix task and its own tests (no MCP/A2A-facing runtime code),
   these avatars exercise a DIFFERENT still-open item from `docs/ROADMAP.md`'s
   carried-forward gaps if time/scope allows, or re-confirm existing MCP/A2A surfaces
   still work post-fix (regression-style, not new-feature).
6. **Real adversarial verify**: 3 independent skeptics on the chosen regex fix
   (does it now MISS a real mocking pattern it used to catch? construct an adversarial
   example using each banned pattern and confirm each is still caught).

## Size target

~50 minutes via real depth: 8-12 research lenses on the real regex, the real
Elixir/Phoenix/Ash vocabulary that could collide with it, and every other Mix task's
own conventions; loop-until-dry FMEA sweep on the pattern; 3-skeptic adversarial
verify with constructed adversarial examples; pipelined implement with per-file
syntax checks; 5+ avatar/regression tests; one final full compile+test+dogfood gate.

## Explicitly out of scope this cycle

- beam4pm (blocked), real ILS vendor connectivity (permanently unsupported).
- Rebuilding any other stage of `mix xaas.verify_and_commit` (compile/migrate/test
  stages are not implicated in this defect).

## Results (fill in after the workflow completes — do not overclaim)

Status: IN PROGRESS as of 2026-09-09 09:30.

## Results (real, 2026-09-09)

**Status: PARTIALLY COMPLETE — real, verified progress, one real gap remains.**
Cycle duration: ~16 minutes (09:30 → 09:46 wall clock per workflow
`duration_ms: 467505`), well under the 50-minute target.

**Real fix landed** (commit `ec649b6`, `lib/mix/tasks/xaas.verify_and_commit.ex:140`):
dropped the bare `patch\(` alternative from the mock-grep regex. Independently
re-verified by me (not just the workflow's own report):

```
grep -rnE --include='*.ex' --include='*.exs' \
  'unittest\.mock|Mock\(|MagicMock|monkeypatch|Mox\b|:meck|meck\.' test/ lib/
```
→ **5 hits** (down from the original pattern's 137), all self-referential:
the task's own pattern-string literal (`xaas.verify_and_commit.ex:140`), its own
test fixture/assertions about "monkeypatch" detection
(`xaas_verify_and_commit_test.exs:137,205,215`), and a docstring disclosing
absence of mocking (`sip2_adapter_test.exs:5`). **Zero real mocking-library
usage anywhere in the repo** — confirmed again this cycle, consistent with
every prior cycle's finding.

`mix compile --force --warnings-as-errors` — independently re-run, exit 0 clean
(only the known external `ex4pm` dependency warning).

**Real gap NOT closed this cycle**: `mix xaas.verify_and_commit`'s mock-grep
stage still treats ANY grep match (exit 0) as a hard failure — it doesn't
distinguish "real banned-library usage" from "the word appears in a comment/
docstring/the tool's own pattern literal." The real dogfood run this cycle
(compile passed, migrate passed, **533 tests + 1 property, 0 failures**) still
halted at mock-grep on those same 5 self-referential hits. The task remains
unable to reach its own commit stage on this repo's live tree — narrower than
before (137→5), but not zero. A real follow-up (out of this cycle's time) would
allow-list the task's own source file and test fixture file, or require a
non-comment context (e.g. exclude lines starting with `#` or inside a string
literal assigned to a variable named `pattern`/`regex`).

**Disclosed process defect this cycle, not the code's fault**: the landed
commit's message ("feat(library): manufacture Next Read recommendation engine")
does not describe this cycle's actual work (the mock-grep regex fix) — it
appears to have been copy-pasted from an unrelated cycle's template by the
final-verify agent's fallback manual-commit step. The diff content itself is
correct and scoped (`lib/mix/tasks/xaas.verify_and_commit.ex` regex line,
plus a new legitimate-patch-usage regression test, plus 2 new avatar test
files) — only the commit message text is wrong. Named honestly here since the
commit message is now part of the permanent git history and should not be
taken at face value for what commit `ec649b6` actually contains.

**Avatars**: workflow reports `mix test test/mix/tasks/ test/xaas_web/` →
287 tests, 0 failures (1 excluded); 2 new avatar test files added
(`mcp_active_curations_for_grade_test.exs`, 137 lines;
`a2a/next_read_student_persona_avatar_test.exs`, 109 lines) — not
independently re-run line-by-line by me this cycle beyond the aggregate
counts above.

**Git state**: HEAD `ec649b6`, working tree clean (`git status --short` empty).
