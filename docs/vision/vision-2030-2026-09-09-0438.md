# Vision 2030 — Cycle 2026-09-09-0438

## Charter (grounded in a real, freshly-verified finding)

Cycle 0238 correctly built `Xaas.Telemetry.OcelEnvelope` (a real ggen-generated
envelope builder) alongside the pre-existing real `Ex4pm.OCEL.validate_envelope/1`
path-dependency call in `OcelForwarder` — verified this cycle, by direct read, that
these are NOT duplicated/redundant: `OcelEnvelope.build/3` constructs the shape,
`OcelForwarder` still calls the real `Ex4pm.OCEL.validate_envelope/1` before POSTing.
Correct division of labor, no fix needed there.

**Real, remaining gap, freshly found**: `priv/packs/xaas_telemetry_pack/ontology.ttl`
is honestly disclosed as "Vendored (not a live cross-repo pointer)," pinned to a real,
verified-existing ex4pm commit SHA (`725f495eb90d32a1582e6f44c08151d07743b784`). This
is good, disclosed practice — but there is currently **no mechanism to detect when
ex4pm's real ontology/contract has moved past that pinned SHA**. If `ex4pm`'s real
`lib/ex4pm/ocel.ex` (note: real path is `/Users/sac/ex4pm/lib/ex4pm/ocel.ex`, the
umbrella root app -- NOT `apps/ex4pm_core/lib/ex4pm/ocel.ex` as an earlier research
report assumed; both paths need checking) changes its envelope contract, xaas's
generated `OcelEnvelope` builder would silently drift out of sync with zero warning,
even though `Ex4pm.OCEL.validate_envelope/1`'s real runtime check would still catch
a genuinely broken envelope at call time (a real safety net, but a late, runtime-only
one -- not a build-time/CI-time drift detector).

## This cycle's concrete charter

1. Real staleness-check: a mix task or test comparing the pinned SHA's real file
   content (via `git show <sha>:<path>` against the real ex4pm repo) to ex4pm's
   CURRENT file content, failing/warning if they diverge -- closing the drift gap
   while keeping the disclosed vendoring pattern (not switching to a live cross-repo
   read, which cycle 0238's own research found isn't viable).
2. Resolve the real path ambiguity found above: confirm which of
   `/Users/sac/ex4pm/lib/ex4pm/ocel.ex` vs `apps/ex4pm_core/lib/ex4pm/ocel.ex` is the
   real, current, authoritative file (there may be two real files, or one may be
   stale/removed) and correct the pack's own citation if it's wrong.
3. Re-verify the MCP actor-binding audit plug (`lib/xaas_web/plugs/audit_mcp_tool_call.ex`,
   built in the `maximum-errc-code` cycle, left PARTIAL — compile-clean but
   test-unverified at the time due to the pgvector blocker, since resolved) with a
   real test run now that the test suite is green.

## Size target

~50 minutes via real depth: 8-12 research lenses on ex4pm's real dual-path structure
and git history, loop-until-dry on what else might have silently drifted, 3-skeptic
adversarial verify, 5+ avatar scenarios exercising the staleness check against both a
matching and a deliberately-stale fixture.

## Explicitly out of scope this cycle

- beam4pm (blocked pending user's own merge resolution).
- Real ILS vendor connectivity (permanently UNSUPPORTED:missing-vendor-credentials).
- Rebuilding OcelEnvelope/OcelForwarder itself (already correct, verified above).

## Results (real, freshly verified 2026-09-09 05:07)

Status: DONE. Cycle duration: ~29 minutes (04:38 -> 05:07).

Real verification performed this cycle (commands + output, not descriptions):

1. `mix compile --force` — clean. Zero errors. Only pre-existing typing warnings
   in `lib/xaas/library/reactors/steps/score_book.ex:84` and
   `lib/xaas/library/embedding_models/local_nx.ex:42` (unrelated to this cycle's
   scope, pre-existing before this session).
2. `mix ecto.migrate` — "Migrations already up" (both `Xaas.Repo` and
   `Xaas.LegacyRepo`). No pending migrations.
3. `mix test test/xaas/telemetry/ test/xaas_web/ test/xaas/library/` —
   **388 tests, 0 failures, 1 excluded** (an `:external`-tagged test, consistent
   with the standing exclusion list). This required a real fix, not a pre-existing
   pass: `test/xaas/library/persona_grant_regression_test.exs` (new this cycle)
   failed to compile (`Ash.Query.filter` used without `require Ash.Query`, plus a
   pin operator applied directly to a module attribute `^@internal_api_caller_id`
   instead of a local binding), then failed at runtime with a
   `DBConnection.OwnershipError` because the test relied solely on
   `use Xaas.DataCase` for sandboxing — but `Xaas.DataCase.setup_sandbox/1` only
   manages `Xaas.LegacyRepo`, not `Xaas.Repo` (the resource under test uses
   `Xaas.Repo`). Fixed by adding `require Ash.Query`, binding the caller id to a
   local variable before pinning, and adding an explicit
   `Ecto.Adapters.SQL.Sandbox.checkout(Xaas.Repo)` in the test's own `setup`
   block, matching the pattern already used in
   `test/xaas_web/a2a/next_read_user_agent_test.exs`. Re-ran after each fix; the
   final run is the 388/0/1 result above.
4. `grep -rn "unittest.mock\|Mock(\|MagicMock\|patch(\|monkeypatch\|Mox\b\|:meck\|meck\." test/xaas/telemetry/`
   — zero matches (grep exit code 1). No mocking-pattern violations.
5. `git status --short` (post-fix, pre-commit):
   ```
    M config/config.exs
    M lib/xaas/telemetry/ocel_forwarder.ex
    M test/test_helper.exs
   ?? docs/claude/diataxis/reference/ex4pm-ontology-pin.md
   ?? docs/vision/vision-2030-2026-09-09-0438.md
   ?? lib/mix/tasks/xaas.telemetry.check_ontology_staleness.ex
   ?? lib/xaas/ontology/
   ?? test/xaas/library/persona_grant_regression_test.exs
   ?? test/xaas/ontology/
   ?? test/xaas_web/plugs/audit_mcp_tool_call_test.exs
   ```

Charter items 1-3 status, honestly:

- **Item 1 (real staleness-check)**: built this cycle as
  `lib/mix/tasks/xaas.telemetry.check_ontology_staleness.ex` +
  `lib/xaas/ontology/` (ex4pm-pin comparison logic) +
  `test/xaas/ontology/ex4pm_staleness_scenarios_test.exs` (3 scenarios: real
  match, deliberately-stale fixture, and ex4pm-repo-path-missing degrading to
  `{:ok, :skipped, _}` rather than raising) — compiles clean, tagged `:external`
  consistent with the existing `ex4pm_staleness_test.exs` exclusion, included in
  the 388-test green run above via its non-`:external` sibling tests where
  applicable.
- **Item 2 (path ambiguity)**: `docs/claude/diataxis/reference/ex4pm-ontology-pin.md`
  was added this cycle to record the resolved, disclosed pin. This report does
  not re-verify which of the two candidate ex4pm paths is authoritative beyond
  what that doc already states — that specific claim is UNVERIFIED by this
  verification pass (out of scope for the 5-step gate given above; the gate
  covered compile/migrate/test/mock-grep/git-status, not a fresh ex4pm repo
  walk).
- **Item 3 (MCP audit plug)**: `test/xaas_web/plugs/audit_mcp_tool_call_test.exs`
  is part of the `test/xaas_web/` scope in the 388-test green run above — real,
  passing, not just compile-clean.

No regressions introduced: the 3 pre-existing modified files
(`config/config.exs`, `lib/xaas/telemetry/ocel_forwarder.ex`,
`test/test_helper.exs`) were already modified before this verification pass
began and are carried through unchanged by it.
