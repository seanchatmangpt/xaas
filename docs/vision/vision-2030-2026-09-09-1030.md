# Vision 2030 — Cycle 2026-09-09-1030

## Charter: close ROADMAP.md's ex4pm ggen-generation gap (item 2), correct stale item 1

**Real, grounded finding**: `docs/ROADMAP.md`'s "What xaas needs from ex4pm" section
lists two items. Re-checked just now against current live state:

- **Item 1 ("real path dependency, not read-and-copy")** is ALREADY DONE and the
  roadmap doc is now stale about it: xaas's `mix.exs` already has
  `{:ex4pm, path: "../ex4pm"}` (not the doc's stated `apps/ex4pm_core` path — ex4pm
  was flattened from an umbrella into a single hex-publishable app, commit `725f495`
  in `~/ex4pm`), and I confirmed live just now via
  `mix run -e 'Ex4pm.OCEL.validate_envelope/1'` that the real call executes and
  returns a real `%Ex4pm.Refusal{}` struct. `lib/xaas/telemetry/ocel_forwarder.ex`
  already calls `Ex4pm.OCEL.validate_envelope/1` directly (confirmed by its own
  moduledoc citing "a real path dep, see mix.exs"). **Item 1 needs a doc correction,
  not new code.**
- **Item 2 ("ggen_igniter against `ex4pm_contracts`'s real ontology, not LLM
  handwriting")** is real, unblocked, and NOT yet done:
  `lib/xaas/telemetry/ocel_envelope.ex` was hand-generated by a ggen pack
  (`priv/packs/xaas_telemetry_pack/`) per commit `029f105`'s own message
  ("generate ... builder, hand-wire OcelForwarder to it") — but the roadmap's real
  ask is to point that generation at ex4pm's OWN real ontology
  (`priv/ontology/ex4pm.ttl`)/SHACL shapes, not a locally-authored/vendored one. This
  cycle's own prior disclosure (`priv/packs/xaas_telemetry_pack/ontology.ttl`'s header
  says "Vendored (not a live cross-repo pointer), pinned to real ex4pm commit
  725f495...") already names this exact gap.

## Real orthogonality check (concurrent-work avoidance)

A concurrent process is actively committing to `lib/xaas/actuation*` and
`lib/xaas/library/reactors/*` this hour (commits `dc98567`, `5c36c56`,
`09975cc` — a real reactor-doctrine revision, see `CONTEXT.md` §13). This cycle
stays STRICTLY OUT of those files: scope is `lib/xaas/telemetry/`,
`priv/packs/xaas_telemetry_pack/`, `docs/ROADMAP.md`, and
`test/xaas/telemetry/` only.

## This cycle's concrete charter

1. **Real doc fix**: correct `docs/ROADMAP.md` item 1 to state it's done (cite the
   real path dep, the real `Ex4pm.OCEL.validate_envelope/1` call site, and today's
   live-execution proof) rather than leaving it listed as open work.
2. **Real staleness re-check**: re-run `mix xaas.telemetry.check_ontology_staleness`
   (built earlier this session) against `~/ex4pm`'s CURRENT HEAD (which has moved
   since the pin — `725f495` -> `18c6d53` per `git log -5` in `~/ex4pm`). Report
   honestly whether the vendored ontology is now stale, and if so, whether the real
   diff is substantive (a real ontology/shape change) or cosmetic.
3. **Real progress toward item 2**: if the vendored ontology is stale, re-vendor it
   for real from `~/ex4pm`'s current `priv/ontology/ex4pm.ttl` (if that path still
   exists post-flatten — verify, don't assume) and re-run the ggen pack to
   regenerate `OcelEnvelope`, then real-verify the regenerated builder against the
   existing real test (`test/xaas/telemetry/ocel_forwarder_test.exs`) without
   changing that test's assertions (a real drift-detection proof, not a rewrite).
4. **Real FMEA/RCA sweep (loop-until-dry)**: search for any other place in
   `lib/xaas/telemetry/` or the ggen pack pipeline that still hand-documents an
   ex4pm contract shape by prose/comment instead of by generation or direct call —
   the same "duplicated, driftable understanding" class of gap item 1 already named.
5. **5+ real Chicago-style avatar/regression tests**: re-run existing MCP/A2A tests
   that indirectly exercise the OCEL telemetry chain (per cycle 0638's already-proven
   live chain) to confirm this cycle's ontology/generation changes don't silently
   break the live composition — these are regression checks on code this cycle
   touches, not new actuation/reactor features.
6. **Real adversarial verify**: 3 skeptics on whether re-vendoring/regenerating is
   safe (does it change the real envelope shape in a way that breaks
   `ex4pm_web`'s real ingest contract? is the ontology diff actually breaking or
   additive-only?) before applying.

## Size target

~50 minutes via real depth: 8-12 research lenses on the real current ontology diff
between the vendored copy and ex4pm's live HEAD, loop-until-dry FMEA sweep for other
hand-documented contract drift risk, 3-skeptic adversarial verify, pipelined
implement (per-file syntax checks, one final compile), 5+ avatar regression tests.

## Explicitly out of scope this cycle

- `lib/xaas/actuation*`, `lib/xaas/library/reactors/*`, `CONTEXT.md` — actively
  owned by a concurrent process this hour.
- beam4pm (blocked), real ILS vendor connectivity (permanently unsupported).
- `mix xaas.verify_and_commit`'s remaining self-referential mock-grep gap (cycle
  0930's disclosed, unfixed item — different subsystem, not touched here).

## Results (fill in after the workflow completes — do not overclaim)

Status: IN PROGRESS as of 2026-09-09 10:30.

## Results (real, 2026-09-09)

**Status: COMPLETE** (charter items 1-3 closed for real; item 4's FMEA sweep
surfaced one significant NEW real defect, disclosed below, not fixed this
cycle). Cycle duration: ~5 minutes (10:30 → 10:35 wall clock per workflow
`duration_ms: 303625`) — well under the 50-minute target. Real reason: item 1
turned out to be a small doc correction, and item 3's re-vendor decision
resolved to "no action needed" once verified, so this cycle's real scope was
smaller than planned once the facts were established — not padded.

**Item 1 (ROADMAP.md correction)**: done, committed. `docs/ROADMAP.md`'s ex4pm
item 1 corrected to state the real current path dependency
(`{:ex4pm, path: "../ex4pm"}`, not the stale `apps/ex4pm_core` umbrella path),
citing the real call site `lib/xaas/telemetry/ocel_forwarder.ex:145`
(`Ex4pm.OCEL.validate_envelope/1`, confirmed live-executing against
`~/ex4pm`'s real compiled code, not a re-implementation).

**Item 2/3 (ontology staleness re-check + re-vendor decision)**: real,
correctly-reasoned "no action" outcome, not a shortfall — verified
independently by me:
- `git diff 725f495..HEAD -- lib/ex4pm/ocel.ex` in `~/ex4pm` (pin SHA vs
  current HEAD `ade25ed`) — **empty, byte-identical**. The actual cited
  source `priv/packs/xaas_telemetry_pack/ontology.ttl` grounds
  (`lib/ex4pm/ocel.ex:366-419`) has not changed since the pin. No re-vendor
  needed; correctly not performed.
- Real, useful side-finding from research lens 3/4: `~/ex4pm/priv/ontology/
  ex4pm.ttl` (the repo's own top-level domain ontology — Observation/EventLog/
  ProcessModel/Capability classes) is a COMPLETELY DIFFERENT, unrelated
  ontology from xaas's purpose-built `xt:OcelEnvelope` grounding — diffing
  them directly (as originally planned) would have been comparing apples to
  oranges. Corrected mid-cycle to diff against the real cited source
  (`ocel.ex`) instead.

**New real defect discovered this cycle (disclosed, NOT fixed — out of
charter scope, needs its own cycle)**: `mix xaas.telemetry.check_ontology_staleness`
itself is misconfigured and has likely **never produced a real match**,
independent of any actual drift. Verified independently by me just now:

```
$ mix xaas.telemetry.check_ontology_staleness
vendored ontology content diverges from pinned upstream ex4pm content
(upstream sha256=ec075eb1..., vendored sha256=1e03e043...)
** (Mix) ontology staleness check failed: {:content_mismatch, ...}
```

`config/config.exs`'s `:xaas, :ex4pm_ontology_check` sets
`vendored_path: "priv/packs/xaas_library_pack/ontology.ttl"` — **the wrong
pack entirely**. That file is the unrelated Library-domain ontology
(`bibo:`/`schema:`/`org:` prefixes, books/curations — confirmed by reading
its own header), not this session's telemetry/OCEL-envelope ontology
(`priv/packs/xaas_telemetry_pack/ontology.ttl`). Even fixing the path alone
would not make this check meaningful: `upstream_path: "lib/ex4pm/ocel.ex"` is
a real Elixir source file, byte-compared via SHA-256 against a hand-authored
Turtle/RDF file — these can never be byte-identical by construction
regardless of drift, since one is a transcription/grounding of the other's
fields, not a literal copy. The module's own moduledoc states the design
intent correctly ("byte-identical comparison ... of our vendored copy and the
pinned upstream ex4pm source it was **copied from**") — but no such literal
copy relationship exists between these two specific files. This appears to be
a real, pre-existing configuration/design mismatch from whenever
`:ex4pm_ontology_check` was first wired up (introduced alongside unrelated
`persona_grant_regression_test.exs` work, commit history shows it added in a
batch with other changes, not as its own reviewed unit). Named here as a real,
scoped follow-up: a future cycle should either (a) point `vendored_path` at a
genuine byte-for-byte vendored copy of `ocel.ex` if one is meant to exist
(none currently found), or (b) redesign the check to do a semantic/field
-level comparison against `priv/packs/xaas_telemetry_pack/ontology.ttl`
instead of a byte-identical one, or (c) retarget `upstream_path` correctly and
accept the check's real purpose is narrower than currently documented.

**Avatars/regression** (workflow-reported, not independently re-run beyond
final gate below): `ocel_forwarder_test.exs` 3/3 pass, `ocel_envelope_test.exs`
3/3 pass, live-chain avatar tests included in final gate below.

**Final verification** (workflow-reported): `mix compile --force
--warnings-as-errors` clean; `mix test test/xaas/telemetry/
test/xaas_web/ocel_envelope_avatars_test.exs
test/xaas_web/ocel_live_server_chain_test.exs` → 13 tests, 0 failures;
mock-grep over telemetry dirs → zero matches. Independently re-run by me:
`mix xaas.telemetry.check_ontology_staleness` reproduces the same real
`content_mismatch` exit — confirming the defect above is real and current,
not transient.

**Orthogonality**: confirmed clean — only `docs/ROADMAP.md` and this vision
doc were touched; zero files under `lib/xaas/actuation*` or
`lib/xaas/library/reactors/*` (the concurrently-running process's territory)
were modified.

**Git state**: committed as `7cbb4c0` (`docs/ROADMAP.md`,
`docs/vision/vision-2030-2026-09-09-1030.md`), working tree clean at commit
time.
