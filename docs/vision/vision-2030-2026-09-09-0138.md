# Vision 2030 — Cycle 2026-09-09-0138

## Charter (grounded, real, carried-forward items — not invented)

Real backlog check performed before picking a theme (`git log`, `docs/ROADMAP.md`,
`docs/vision/vision-2030-2026-09-09-0020.md`, `docs/hddl/`):

**RCA/FMEA carried forward, explicitly named in prior cycles, still open:**
1. `/mcp`'s actor-resolution gap — same root cause as the A2A impersonation gap closed
   in cycle 0020 (`ResolveOrgActor` is a no-op on `/mcp`'s `path_info`, `Book`/
   `Curation` policies are `authorize_if always()`). Named as out-of-scope in cycle
   0020, not yet closed.
2. Workflow-serialization lockfile (`docs/hddl/mechanical-rename.hddl`'s RCA item) —
   still doc-only (`.claude/workflow-lock-guard.md`), no enforced mutex.
3. **Real functional gap identified in this session's direct Q&A** (not from a
   workflow): `Checkout.return` never triggers `HoldRequest.fulfill` — a returned book
   does not automatically fulfill the next waiting hold. `HoldRequest.fulfill` itself
   does cross-resource orchestration (bumps `Book.available_copies`) via a bare
   `Ash.Changeset.after_action` callback, not a receipted Reactor — same TOCTOU class
   as the already-fixed borrow/return atomicity bug.
4. `Checkout.borrow`'s actuation-gate asymmetry — only the A2A call site routes through
   `Xaas.Actuation.run/4`; `reader_live.ex`'s LiveView checkout handler still calls
   `Ash.create()` directly.

## This cycle's charter: item 3 (ReturnAndFulfillReactor) as primary, item 1 (MCP
   actor-binding) as secondary if scope allows

Item 3 is the highest real value: it's a missing *functional* link in the product
(returns don't fulfill holds), not just a hardening gap. Item 1 reuses the exact
`docs/hddl/actor-binding-fix.hddl` method already proven in cycle 0020 — cheaper to
execute a second time.

## Size target (per standing cron instruction)

~50 minutes real wall-clock, hit via real depth: 8-12 research lenses, loop-until-dry
FMEA/RCA scan on the return/fulfill/actuation surface, a 3-skeptic adversarial verify
pass on findings before implementing, 5+ real MCP/A2A avatar scenarios, multiple real
implementation batches. Explicit instruction to this cycle's workflow: if real scope
runs out before 50 minutes, say so honestly here rather than padding.

## Explicitly out of scope this cycle

- No ggen_igniter/ex4pm_contracts ontology-generation work (ROADMAP.md item 2) — real,
  separate scope, not blocking this cycle.
- No beam4pm work — still blocked pending user's own merge resolution per this
  session's explicit hold.
- No billing/ledger integration — repeatedly confirmed out of scope across cycles.

## Results (final verification pass, 2026-09-09)

Status: COMPLETE, with one real, named test failure carried forward. This is the
single, real, full-repo compile+test verification pass for this cycle — not a
description of it.

**1. `mix compile --force`**: found and fixed a real compile error during this pass.
`lib/xaas/library/reactors/recommendation_pipeline_reactor.ex`'s
`:extract_accepted_book_ids` step called `Ash.Query.filter(user_id == ^user_id and
accepted == true)` without `require Ash.Query` in scope, so the macro never expanded
— Elixir compiled the filter expression as literal code, producing `undefined
variable "accepted"` and `misplaced operator ^user_id`. Confirmed reproducible twice
against a clean `_build/dev/lib/xaas` (not a stale-cache artifact). Fix: added
`require Ash.Query` to the module. Re-verified clean on a second from-scratch
`rm -rf _build/dev/lib/xaas && mix compile --force` — `Generated xaas app`, zero
errors, zero warnings from `xaas` itself (only the pre-existing unrelated
`Application.get_env/2` warning from the `ex4pm` dependency's own `mix.exs`).

**2. `mix ecto.migrate`**: `Migrations already up` — the new
`20260909042306_add_deck_capabilities.exs` migration (and everything else) was
already applied to the running Postgres (port 50730) before this pass ran. No
pending migration to apply.

**3. `mix test test/xaas/library/ test/xaas_web/`**: **349 tests, 1 failure, 1
excluded.** The one real failure:

- `XaasWeb.A2A.ReturnHoldCascadeAvatarsTest` — "avatar 3: return with multiple active
  holds -- only the oldest is fulfilled" (`test/xaas_web/a2a/
  return_hold_cascade_avatars_test.exs:180`): `assert first_after.status ==
  :fulfilled` got `:active` — the oldest hold is never marked fulfilled at all after
  a `Checkout.return`. Reproduced in isolation
  (`mix test test/xaas_web/a2a/return_hold_cascade_avatars_test.exs:179`, seed 0):
  same failure, deterministically, not a flake. Root cause not yet isolated in this
  verification pass (candidates inspected but not confirmed: the `after_action` in
  `Xaas.Library.Changes.FulfillNextHold` discards the result of the inner
  `Ash.update(authorize?: false)` call on the hold — if that update itself errors
  silently, the surrounding `return` action would still commit and the checkout-level
  assertions in avatars 1/2 could still pass while avatar 3's status assertion fails).
  This is real new-code (untracked `lib/xaas/library/changes/fulfill_next_hold.ex`,
  `lib/xaas/library/reactors/`, `test/xaas_web/a2a/
  return_hold_cascade_avatars_test.exs`) from earlier in this same cycle, not a
  regression from this verification pass's own `require Ash.Query` fix (confirmed:
  the failure reproduces identically before and after that fix, and the touched file
  is unrelated to hold fulfillment). ALIVE: return/borrow/inventory atomicity and two
  of three avatar scenarios. NOT YET ALIVE: avatar 3's multi-hold-queue fulfillment
  ordering — the item-3 charter goal ("a returned book automatically fulfills the
  next waiting hold") is real and partially implemented, but the queue-order
  guarantee is unverified/failing as of this pass.

**4. Mock grep** (`test/xaas/library/`, `test/xaas_web/a2a/`): zero matches. Clean —
no `unittest.mock`/`Mock(`/`MagicMock`/`patch(`/`monkeypatch`/`Mox`/`:meck` usage.

**5. `git status --short`**: real changes present — this is a large, already-manufactured
diff from earlier in the cycle (not produced by this verification pass itself, apart
from the one `require Ash.Query` line): modified `lib/xaas/actuation.ex`,
`lib/xaas/library.ex`, `lib/xaas/library/checkout.ex`, `lib/xaas/library/
hold_request.ex`, `lib/xaas/library/ranker.ex`, `lib/xaas_web/a2a/
next_read_user_agent.ex`, `lib/xaas_web/components/layouts/root.html.heex`,
`lib/xaas_web/live/next_read/reader_live.ex`, `mix.exs`/`mix.lock`,
`package.json`/`package-lock.json`, `templates-hooks/terraform-validate.txt.tmpl`,
and three test files; deleted `e2e/next-read-ml.spec.js` and `playwright.config.js`
(replaced by `.cjs` equivalents); new untracked: `CONTEXT.md`, `docs/hddl/*.mmd`,
`docs/hddl/next-read.hddl`, this vision doc, `e2e/next-read-ml.spec.cjs`,
`e2e_petal_screenshot.png`, `lib/xaas/actuation/middleware/`, `lib/xaas/hddl/`,
`lib/xaas/library/changes/fulfill_next_hold.ex`, `lib/xaas/library/reactors/`,
`playwright.config.cjs`, `test/bdd/`, `test/xaas/library/reactors/`,
`test/xaas/library/return_fulfills_hold_test.exs`,
`test/xaas_web/a2a/return_hold_cascade_avatars_test.exs`, `tsconfig.json`,
`vitest.config.ts`.

**Size target**: not directly measurable from within this verification-only
sub-task — this pass has no visibility into the cycle's actual start time or the
workflow's own `duration_ms`. Stated honestly rather than guessed: this workflow
stage itself (compile fix, migrate, full test run, mock grep, status review, this
write-up) ran on the order of a few minutes of real tool time, not 50; whether the
cycle as a whole (research + implementation stages before this one) hit the ~50-minute
target is not something this stage can attest to.

**Honest ALIVE vs. carried-forward summary**:
- ALIVE and verified in this pass: full-repo compile (315 files, zero errors after
  the fix above), migrations current, 348 of 349 targeted tests passing, zero
  mock/interaction-fake usage in the targeted test dirs.
- PARTIAL: item 3's core feature (hold auto-fulfillment on return) — works for the
  single-active-hold case (avatars 1/2 pass) but fails the multi-hold
  oldest-first-ordering case (avatar 3) as of this commit. Needs a follow-up
  `systematic-debugging` pass on `Xaas.Library.Changes.FulfillNextHold` and
  `HoldRequest`'s `:fulfill` action before item 3 can be called done.
- Carried forward, untouched this cycle per the charter's explicit scope: item 1
  (`/mcp` actor-resolution gap), item 2 (workflow-serialization lockfile), item 4
  (`Checkout.borrow` actuation-gate asymmetry in `reader_live.ex`).

## ERRC Pass — 2026-09-09 (mcp-actor-binding + workflow-lockfile)

**Scope:** two ERRC items — (1) MCP tool-call audit logging binding, (2) a real
workflow compile-lock helper script.

### Closed for real this pass

- **mcp-actor-binding**: `XaasWeb.Plugs.AuditMcpToolCall` created
  (`lib/xaas_web/plugs/audit_mcp_tool_call.ex`), wired into the `/mcp` pipeline
  in `lib/xaas_web/router.ex` after `:resolve_org_actor`. Writes one
  `Xaas.Operations.AuditLogEntry` per `/mcp` request via
  `Ash.Changeset.for_create(:create, ..., authorize?: false) |> Ash.create()`,
  caller identity = truncated SHA-256 of the bearer token. Audit-write failure
  is logged, non-blocking. Syntax verified via `Code.string_to_quoted`
  (both files parse). **Not yet verified by a real compiled build or a real
  test run against `/mcp`** — `mix compile --force` at FinalVerify time did
  complete cleanly (no errors, only pre-existing warnings), so the code is at
  minimum ALIVE at the compile level, but no `mix test` exercised this plug
  because the full suite is currently blocked (see Blocked, below).
- **workflow-lockfile**: `.claude/workflow-compile-lock.sh` created and real
  two-process mutex behavior verified (acquire/check/release, stale-PID
  reclaim) via actual background subprocesses, not mocks.
  `.claude/workflow-lock-guard.md` updated to point at the real helper.
  `bash -n` syntax-checked; no `mix compile` dependency for this item (it's a
  shell script, not Elixir).

### Blocked (real, not rounded up)

- **Full verification gate could not complete.** `mix compile --force`
  succeeded cleanly. `mix ecto.migrate` and `mix test test/xaas/library/
  test/xaas_web/` both hard-fail at
  `20260909090808_add_pgvector_book_embedding_extensions_1.exs` with
  `ERROR 0A000 feature_not_supported: extension "vector" is not available`
  on this Postgres instance. Those pgvector migration files are untracked
  and belong to a concurrent pgvector workflow's territory, not this pass —
  they were not created or touched by this pass's agents. Because
  `mix test`'s alias chain runs `ecto.migrate` before `test`, this blocks
  the entire suite for every workflow sharing this tree right now, including
  this one. **No test evidence exists yet for `audit_mcp_tool_call.ex`** —
  it must be re-verified with a real `mix test` run once the pgvector
  extension is available or that migration is fixed/reverted by its owning
  workflow.
- Working tree currently also shows real uncommitted Explainer/SIP2 files
  (`lib/xaas/library/explainer.ex`, `lib/xaas/library/ils_repo*`,
  `test/xaas/library/explainer_test.exs`, etc.) and modified
  `config/config.exs`, `mix.exs`, `lib/xaas/repo.ex`,
  `lib/xaas/library/book.ex`, `lib/xaas/library/ranker.ex`,
  `priv/resource_snapshots/repo/extensions.json` — none created or edited by
  this pass. These were left out of this commit deliberately (staged only
  this pass's own files) to avoid attributing a concurrent workflow's
  in-progress, untested changes to this ERRC receipt.

### grep for mock usage (test/xaas/library/, test/xaas_web/a2a/)

`grep -rn "unittest.mock\|Mock(\|MagicMock\|patch(\|monkeypatch\|Mox\b\|:meck\|meck\." test/xaas/library/ test/xaas_web/a2a/`
→ one hit, a comment in `test/xaas/library/ils_repo/sip2_adapter_test.exs:5`
stating *"No `Mox`/`:meck`/`patch` anywhere in this"* — prose disclaiming
mock usage, not actual mock usage. No real violation.

### Honest status labels

- `audit_mcp_tool_call.ex` + router wiring: **PARTIAL** (compiles clean,
  unverified by test — blocked by unrelated infra, not by this code)
- `.claude/workflow-compile-lock.sh`: **ALIVE** (real subprocess-verified
  mutex behavior, no Elixir/DB dependency)
- Full-suite `mix test test/xaas/library/ test/xaas_web/`: **BLOCKED**
  (pre-existing infra gap outside this pass's scope — pgvector extension
  missing on this Postgres, migration owned by a concurrent workflow)
