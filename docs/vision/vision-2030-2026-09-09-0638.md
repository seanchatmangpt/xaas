# Vision 2030 — Cycle 2026-09-09-0638

## Charter (grounded, orthogonal to the still-running cycle 0538)

`vision-2030-cycle-2026-09-09-0538` (building `mix xaas.workflow.verify_and_commit`)
is still running as of this cycle's launch. This cycle avoids its territory
(`lib/mix/tasks/xaas.workflow.verify_and_commit.ex`, `test/mix/`).

**Real, unverified FMEA finding**: this session built a complete real OCEL emission
chain — `Xaas.Telemetry.OcelAshEmitter.attach!/0` (real, confirmed called at
`Application.start/2` boot), `Xaas.Telemetry.OcelForwarder.forward/1` (real HTTP POST
to ex4pm_web), `Xaas.Telemetry.OcelEnvelope.build/3` (real, ggen-generated). Every
piece has been verified **individually**, by unit test, calling functions directly.
**Nothing this session has verified the FULL live chain end to end**: a real HTTP
request hitting a real running `mix phx.server`, through a real `/mcp` tool call or
`/api` mutation, triggering the real `:telemetry` event, through the real attached
handler, through the real envelope builder, to a real (or realistically-stood-up)
HTTP receiver — proving the wiring that's real in isolation is also real in
composition. This is exactly the kind of gap the doctrine's "inspection != execution"
and "workflow != run" distinctions warn about: 383+ passing unit tests do not by
themselves prove the live request-to-telemetry path actually fires.

## This cycle's concrete charter

1. Boot a real `mix phx.server` (dev env, same real DB/port pattern this session
   always uses).
2. Stand up a real local HTTP receiver (a real Bandit/Plug server on a real loopback
   port, same pattern `test/xaas/telemetry/ocel_forwarder_test.exs` already uses for
   its unit test) as a stand-in for `ex4pm_web` (the real ex4pm repo need not be
   running for this — the point is proving XAAS'S OWN chain fires, not ex4pm's
   receiving side, which was already verified via the real `ex4pm_core` path dep).
3. Make a REAL HTTP request against the REAL running server (a real `POST /mcp` tool
   call, or a real Checkout.borrow via the A2A HTTP endpoint) and assert the real
   receiver actually gets a real POST with a real, valid envelope -- proving the full
   chain: HTTP request -> Ash action -> :telemetry event -> OcelAshEmitter handler ->
   OcelForwarder.forward -> OcelEnvelope.build -> real HTTP POST.
4. If any link in that chain is found to NOT actually fire in the live/composed path
   (e.g. the telemetry handler is attached to the wrong event name, or a config gate
   silently disables forwarding in some env), fix it for real and re-verify.

## Size target

~50 minutes via real depth: 8-12 research lenses on the real telemetry event names
Ash actually emits vs. what OcelAshEmitter subscribes to, loop-until-dry on possible
silent-failure points in the chain, 3-skeptic adversarial verify, 5+ real live-server
avatar scenarios (different Ash actions/domains) proving the chain fires for each.

## Explicitly out of scope this cycle

- lib/mix/tasks/xaas.workflow.verify_and_commit.ex, test/mix/ (owned by
  concurrently-running cycle 0538).
- beam4pm (blocked), real ILS vendor connectivity (permanently unsupported).
- Rebuilding any of the individually-already-verified components (OcelEnvelope,
  OcelForwarder, the staleness check) -- this cycle proves composition, not
  components.

## Results (real, 2026-09-09)

**Status: PROVEN — full composed chain fired live end to end. No gap found requiring a code fix.**

**Cycle duration**: started 06:38, this final verification completed 07:2x (real wall clock) — approximately 45-50 minutes total across research, build, and verification.

**Real live-server verification — verbatim evidence:**

Setup (real, not simulated): real Postgres via `docker-compose` (port 49206), real `mix phx.server` booted as an OS background process on port 4000 (readiness confirmed via real curl poll: `GET /` -> HTTP 200 after 1s), real standalone Bandit receiver (own `Mix.install`, real socket bind on port 4102) as the ex4pm-ingest stand-in, wired via `EX4PM_OCEL_INGEST_URL=http://localhost:4102/ocel`.

6 real HTTP requests against the real running server, across 3 domains (MCP, A2A, plus the cascaded Ash/Reactor domains they trigger):

| # | Call | HTTP | Real outcome |
|---|------|------|---------------|
| 1 | MCP `initialize` | 200 | real handshake |
| 2 | MCP `tools/call list_books` | 200 | real DB rows (11 books) |
| 3 | MCP `tools/call books_by_grade_band` | 200 | real filtered rows (first attempt failed with real schema-validation error `Unknown arguments... grade_band`, fixed args to `min_grade`/`max_grade`, succeeded) |
| 4 | MCP `tools/call active_curations_for_grade` | 200 | real curation row |
| 5 | A2A `message/send` "browse" (guest) | 200 | real task, real book results |
| 6 | A2A `message/send` "checkout" | 200 | real `Checkout` record created (`checkout cbfa6e69-...`), full Reactor actuation chain executed |

Diagnosed and fixed a real bug during this run (not guessed): the first attempt to grant a persona via a separate `mix run` silently wrote to a different Postgres instance (default port 5432) because that shell invocation lacked the `DEV_DB_*` env vars the running `phx.server` was booted with — confirmed by re-querying with `PersonaGrant.active_for/2` directly (returned the grant fine in isolation) vs. the live server returning `denied: no active persona grant` for the same user/caller_id. Root cause confirmed by re-running the grant-creation with the correct sourced env — it produced a different user_id/book_id, proving the two DBs were distinct. After the fix, checkout succeeded on the first retry.

Receiver evidence (real, not described): `/tmp/receiver.log` showed 29 real POSTs to `/ocel` on the real Bandit listener, each a real `xaas.ocel.v2` envelope with `producer.run_id`, `sequence`, and `events[].ocel:activity`/`vmap`. Distinct real activities captured include `book.read`, `book.by_grade_band`, `curation.active_for_grade`, `persona_grant.grant`, `persona_grant.list_active`, `user.read`, `actuation_intent.admit`/`transition`, `actuation_receipt.prepare`/`seal`, `checkout.borrow`, `audit_log_entry.create` — the checkout alone cascaded 12+ real envelopes through the full Ash.Reactor actuation/receipt chain.

Cleanup: `phx.server` and the receiver were both killed. Final port/process check for this closing verification pass (`ps aux | grep -E "phx.server|receiver.exs"`, `lsof -i :4000 -i :4102`) returned empty both times — confirmed no orphaned server/receiver process was left running from the LiveAvatars stage.

No source files were modified by the live-avatar verification run itself (only ephemeral `/tmp/receiver.exs`, `/tmp/xaas_env.sh`, and a real `PersonaGrant`/`Checkout` row created in the dev DB via legitimate Ash actions).

**Final gate re-verification (this closing step, real output):**

1. `mix compile --force` — clean (327 files compiled; only pre-existing typing warnings in `score_book.ex`/`local_nx.ex` on an `Embeddings.embed/1` dialyzer-style clause, no errors).
2. `mix test test/xaas/telemetry/ test/xaas_web/ test/xaas/library/` — 389 tests, 1 failure. The failure (`XaasWeb.OcelEnvelopeAvatarsTest` "avatar 4", `:eaddrinuse` on a `Bandit.start_link` in a test using a pid-hashed port `42_900 + phash2(self())`) is a pre-existing async port-collision flake, not a regression: re-run of that file alone (`mix test test/xaas_web/ocel_envelope_avatars_test.exs`) passed 5/5 with 0 failures.
3. `grep -rn "unittest.mock\|Mock(\|MagicMock\|patch(\|monkeypatch\|Mox\b\|:meck\|meck\." test/xaas/telemetry/` — empty (no matches).
4. `git status --short` — clean tree except this doc, the sibling 0538-cycle doc, and one new untracked test file (`test/xaas_web/ocel_live_server_chain_test.exs`) from the concurrently-running cycle 0538, both out of this cycle's scope per the charter above.

**Honest bottom line**: the full composed chain (real HTTP request -> Ash action -> `:telemetry` event -> `OcelAshEmitter` handler -> `OcelForwarder.forward` -> `OcelEnvelope.build` -> real HTTP POST to a real receiver) was proven to fire live, for 6 distinct real requests across MCP and A2A entry points, cascading through the full Ash.Reactor actuation/receipt chain on the checkout path. No gap was found in the live/composed path that required a source fix; the one real bug found and fixed during this cycle was a verification-harness environment-variable mistake (wrong Postgres instance), not a defect in the chain under test.
