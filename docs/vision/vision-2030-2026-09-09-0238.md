# Vision 2030 — Cycle 2026-09-09-0238

## Charter (grounded, orthogonal to 2 concurrently-running workflows)

Two workflows are currently running against this repo: a debugging pass on the real
Groq `Registry.lookup/2` crash + one reactor test (owns `explainer.ex`, `groq_adapter.ex`,
`recommendation_pipeline_reactor_test.exs`), and `utilize-all-reactor-capabilities`
(owns `actuation.ex`, `circulation_borrow_reactor.ex`, `student_profile_sub_reactor.ex`,
`steps/score_book.ex`, `actuation/middleware/`). Per `.claude/workflow-lock-guard.md`,
this cycle picks a theme touching neither territory.

**Real, unblocked, carried-forward item: `docs/ROADMAP.md`'s "What xaas needs from
ex4pm" item 2** — `ggen_igniter` against `ex4pm_contracts`'s real, hashed, versioned
ontology (`priv/ontology/ex4pm.ttl`, `priv/shacl/ex4pm-shapes.ttl`) should generate
xaas's OCEL envelope builder/validator code, following the same generation pipeline
`xaas_library_pack` already uses for `Xaas.Library` — retiring
`lib/xaas/telemetry/ocel_forwarder.ex`'s hand-written envelope construction, per this
repo's own "framework generator exists -> no LLM handwriting" doctrine. This item was
named as real, scoped, and unblocked in two separate places this session
(`docs/ROADMAP.md`, the ex4pm-role-split discussion) and has not yet been executed.

## Size target

~50 minutes via real depth: 8-12 research lenses on the real ggen_igniter pipeline +
ex4pm_contracts's real ontology shape + OcelForwarder's current real behavior,
loop-until-dry on "what does the generated code need to cover that the hand-written
version does," 3-skeptic adversarial verify before implementing, 5+ real avatar
scenarios exercising OCEL emission through both MCP tool calls and A2A messages.

## Explicitly out of scope this cycle

- Anything under lib/xaas/actuation*, lib/xaas/library/reactors/ (owned by the
  concurrently-running reactor-capabilities workflow).
- lib/xaas/library/explainer*, groq_adapter.ex, recommendation_pipeline_reactor_test.exs
  (owned by the concurrently-running debugging pass).
- beam4pm (still blocked pending user's own merge resolution).
- Real ILS vendor connectivity (permanently UNSUPPORTED:missing-vendor-credentials,
  not actionable).

## Results (fill in after the workflow completes — do not overclaim)

Status: IN PROGRESS as of 2026-09-09 02:38. Workflow launched:
`vision-2030-cycle-2026-09-09-0238`.

## Receipt: Reactor-capability verification pass (2026-09-09 02:40 PT)

- `mix compile --force`: exit 0. Only pre-existing unrelated warnings (Application.get_env in ex4pm dep; two typing-violation notes in score_book.ex/local_nx.ex).
- `mix ecto.migrate`: "Migrations already up" (no pending migrations from this pass).
- `mix test test/xaas/library/ test/xaas_web/`: 366 tests, 1 failure, 1 excluded.
  - Failure: `test/xaas/library/next_read_test.exs:77` — `assert is_list(book.embedding)` fails because `book.embedding` is an `Ash.Vector` struct, not a raw list. Unrelated to this pass's reactor/undo/audit-logger work; pre-existing.
  - Known pre-existing failures named in the task (Groq explainer, one reactor test) were re-run in isolation: `mix test test/xaas/library/explainer_test.exs test/xaas/library/reactors/recommendation_pipeline_reactor_test.exs` → 7 tests, 0 failures. Both currently pass; status changed since they were last recorded as failing (owned by a separate debugging pass, not touched here).
- Mock grep (`test/xaas/library/`): 1 match, in `ils_repo/sip2_adapter_test.exs:5` — a comment stating "No Mox/:meck/patch anywhere in this [test]", not actual mock usage. No real mocking found.
- `git status --short` confirms `explainer.ex`, `groq_adapter.ex`, `recommendation_pipeline_reactor_test.exs` were NOT modified by this pass.

Batch summary (B1-B5): audit_logger halt/event middleware logging with real Reactor-driven test; OCEL undo/cancellation wiring on the actuation reactor (documented compensate-vs-undo semantics decision); circulation borrow reactor hold-create undo wiring plus HoldRequest cancel changeset argument fix (required by Spark DSL verifier); recommendation pipeline reactor converted score-and-rank to a real Reactor `map` step with per-item error isolation; student profile sub-reactor embedding-error handling fix. All verified via real `mix compile --force` and real `mix test` runs, no mocking introduced.

## Final verification receipt — OCEL envelope avatar cycle (2026-09-09, final pass)

Cycle scope actually executed: added real avatar/regression coverage for the OCEL
envelope path (`test/xaas_web/ocel_envelope_avatars_test.exs`, 5 tests) — MCP
`list_books` -> telemetry -> `OcelForwarder` -> `OcelEnvelope.build/3` -> real HTTP
capture -> `Ex4pm.OCEL.validate_envelope/1`; A2A `checkout book` through the same
path; structural/byte-level regression check of `OcelEnvelope.build/3`'s output
against the pre-existing hand-written envelope shape; `ReconcileReactor.plan/1`
idempotency check against `priv/packs/xaas_telemetry_pack`; and
`validate_envelope/1` edge cases (empty events, missing optional fields). This
verification pass did not itself execute the ggen-generation retirement of
`ocel_forwarder.ex` described in the charter above — that remains unexecuted;
only the avatar/regression safety net around the current hand-written behavior was
built and verified.

- `mix compile --force`: clean. Only the same two pre-existing unrelated warnings
  noted above (`score_book.ex:84`, `local_nx.ex:42`) plus one pre-existing
  `ex4pm` dep warning (`Application.get_env/2` discouraged in module body).
- `mix ecto.migrate`: "Migrations already up" (no pending migrations).
- `mix test test/xaas/telemetry/ test/xaas/library/ test/xaas_web/`: 383 tests,
  0 failures, 1 excluded. (Up from the earlier receipt's 366 tests/1 failure —
  the `next_read_test.exs:77` `Ash.Vector` failure recorded above is no longer
  present in this run's scope: it does not reappear in this test path today,
  fixed by concurrent, non-overlapping work per `git status --short` below.)
- Mock grep (`grep -rn "unittest.mock\|Mock(\|MagicMock\|patch(\|monkeypatch\|Mox\b\|:meck\|meck\." test/xaas/telemetry/`):
  zero matches.
- `git status --short` at verification time: only
  `?? test/xaas_web/ocel_envelope_avatars_test.exs` — confirms
  `actuation*`, `reactors/`, `explainer*`, `groq_adapter.ex` were NOT touched by
  this cycle (all prior working-tree modifications from earlier same-day cycles
  were already committed, per `git log`).
- Real cycle duration: not separately timestamped by this final pass; the
  compile+migrate+test sequence above ran in ~40 seconds wall clock
  (test suite: "Finished in 38.7 seconds").
