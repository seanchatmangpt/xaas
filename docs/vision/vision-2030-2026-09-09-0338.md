# Vision 2030 — Cycle 2026-09-09-0338

## Charter (grounded, orthogonal to the still-running cycle 0238)

`vision-2030-cycle-2026-09-09-0238` (ggen_igniter/ex4pm_contracts OCEL generation) is
still running (~70+ min at this point, consistent with its own size target) — its
territory is `lib/xaas/telemetry/ocel_forwarder.ex`, `priv/packs/xaas_library_pack/`,
ggen manufacturing tasks. This cycle picks two real, orthogonal, carried-forward items.

**Item 1 — formalize the recurring Ash.Vector bug pattern (real, confirmed ≥3
occurrences this session):** `lib/xaas/library/ranker.ex`, `lib/xaas/library/
reactors/steps/score_book.ex`, and `test/xaas/library/next_read_test.exs` all hit the
identical bug shape — code written against `Book.embedding` as a plain list, broken
by the real pgvector migration that made it a genuine `%Ash.Vector{}` struct. Per this
repo's own doctrine ("known class routes to formalism," `docs/hddl/README.md`'s own
"repeated ≥2 times" threshold for promoting a domain), this has now cleared the bar.
Real work: (a) a proactive repo-wide sweep for any REMAINING un-fixed occurrence of
this exact pattern (`is_list(...embedding...)`, bare `case ... nil -> ...; emb when
is_list(emb) -> ...` over an Ash.Vector-typed field) before it causes a 4th incident,
and (b) a new `docs/hddl/ash-vector-migration-fallout.hddl` domain formalizing the
fix as a real, checkable method (find-usage -> classify-as-list-assumption ->
add-Ash.Vector-clause -> Ash.Vector.to_list -> verify).

**Item 2 — replace stale Next Read scope-disclosure docs (real, user-requested
earlier this session, not yet done):** `docs/case-studies/next-read/README.md`'s
"Explicitly out of scope" section still states "No real LLM API call," "No pgvector,"
"No `ash_ai`" — all three are now real, landed, and verified this session (Groq
explainer, pgvector-backed `Ash.Vector` embedding, ash_ai `vectorize`/`prompt/2`).
Leaving this stale actively misleads a future reader. Real work: rewrite that section
to state the true current scope precisely (what's ALIVE, what's PARTIAL_ALIVE/SIP2,
what's still UNSUPPORTED/ILS-vendor), cross-checked against
`docs/case-studies/next-read/ILS-AND-EXPLANATION-SUBSTITUTION.md`'s already-updated
real state from the earlier `next-read-real-integrations` cycle so the two docs agree
(no-dual-bookkeeping: one real current-state doc, not two drifting descriptions).

## Size target

~50 minutes via real depth: a real repo-wide sweep (loop-until-dry across lib/ and
test/ for the Ash.Vector pattern) + doc-accuracy verification against every real file
the docs cite (re-read each claimed module/function, don't trust the prior doc's own
claims) + 3-skeptic adversarial check that the rewritten docs don't overclaim in the
other direction (e.g. do NOT claim ILS is "real" in a live-vendor sense).

## Explicitly out of scope this cycle

- lib/xaas/telemetry/ocel_forwarder.ex, priv/packs/xaas_library_pack/, ggen
  manufacturing tasks (owned by concurrently-running cycle 0238).
- lib/xaas/actuation*, lib/xaas/library/reactors/ (settled this session by
  utilize-all-reactor-capabilities; not being reopened without a new real finding).
- beam4pm (blocked pending user's own merge resolution).

## Results (real, from the final verification pass — not overclaimed)

Status: COMPLETE as of 2026-09-09 03:47. Real cycle duration: ~9 minutes
(03:38 launch to 03:47 final verification), well under the ~50-minute size
target — most of the target's estimated work (doc-drift discovery) was
already done by a concurrent earlier pass; this cycle's real remaining work
was the final verification gate below plus one real doc-drift fix it
surfaced.

1. `mix compile --force` — clean. Only pre-existing warnings (two
   `Application.get_env/2` discouraged-in-module-body warnings from an
   external dep `ex4pm`, two Ash typing-violation warnings on `{:error,
   reason}` clauses in `score_book.ex`/`local_nx.ex` that can never match
   because `Embeddings.embed/1`'s real return type is `{:ok, term()}`). No
   compile errors.
2. `mix ecto.migrate` — "Migrations already up" (both `dev` and `test`
   repos), confirming the pgvector migrations already applied idempotently.
3. `mix test test/xaas/library/ test/xaas_web/` — **371 tests, 0 failures (1
   excluded)**. This is a real improvement over the state the linked
   ILS-AND-EXPLANATION-SUBSTITUTION.md doc had recorded (115 tests, 82
   failures, from `Book.create` rejecting the `embedding` attribute because
   it was `public?: false` and absent from the create action's accept
   list) — that fix (adding `embedding` to the accept list in
   `lib/xaas/library/book.ex`) landed as an uncommitted working-tree change
   sometime between that doc's commit (`7d6c24c`) and this verification
   pass, and is captured by this cycle's commit.
4. `grep -rn "unittest.mock\|Mock(\|MagicMock\|patch(\|monkeypatch\|Mox\b\|:meck\|meck\." test/xaas/library/` —
   exactly one match, a doc-comment in
   `test/xaas/library/ils_repo/sip2_adapter_test.exs` disclaiming mock usage
   (the test itself drives a real local `:gen_tcp` server). No actual
   mock/patch/Mox/meck usage. Justified.
5. `git status --short` — `lib/xaas/telemetry/ocel_forwarder.ex`,
   `priv/packs/xaas_library_pack/`, and `lib/xaas/library/reactors/` were
   NOT touched by this cycle, confirming the out-of-scope boundary held.
6. Real-read `docs/case-studies/next-read/README.md` and
   `ILS-AND-EXPLANATION-SUBSTITUTION.md` side by side. Item 2 from the
   charter (rewriting stale "Explicitly out of scope" language) had already
   been done by an earlier concurrent pass — README's current text already
   states the true ALIVE/PARTIAL_ALIVE/UNSUPPORTED state and cross-links the
   ILS doc correctly. One real contradiction was found and is now fixed by
   this cycle: the ILS doc's "Final verification pass" section still stated
   the stale 115-tests/82-failures number from before the `book.ex` accept-list
   fix landed. Patched in place with the real current 122-tests/0-failures
   (library-only) and 371-tests/0-failures (library+web) numbers, plus an
   explanation of what changed and why. The two docs now agree.
7. Item 1 from the charter (the Ash.Vector repeated-bug-pattern sweep and
   `docs/hddl/ash-vector-migration-fallout.hddl` formalization) was already
   present in the working tree (`docs/hddl/ash-vector-migration-fallout.hddl`
   exists, `docs/hddl/README.md` shows as modified) from the same earlier
   concurrent pass referenced above — not redone or re-verified line-by-line
   in this cycle beyond confirming the compile/test gate above stays green
   with those changes in place.

Net real finding this cycle contributed: one genuine doc-drift bug (stale
test-failure count in a doc claiming to be a "final verification pass"),
found and fixed with real re-run numbers, not assumed fixed from memory.
