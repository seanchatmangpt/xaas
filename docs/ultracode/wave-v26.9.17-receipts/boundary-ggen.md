# Boundary Qualification Receipt — ggen / cap-manufacture (r4)

Agent: implementation agent 4/10, SA2A release v26.9.17 wave
Date: 2026-09-17
Mission: `qualify-boundary(ggen, cap-manufacture)` — is ggen = CONSTRUCT real here: pack.toml → ontology → templates → gates → manufactured artifacts?

## Pinned subject

- Mission pin: `03ceb0df6` on `feat/marketplace-sparql-semantic-search`
- Observed at start: `03ceb0df6f5e9506f0345a46c4807063963d3334`, clean tree — NO drift.
- Work branch (repairs): `fix/ggen-v26.9.17-boundary`, cut from the pinned SHA. Never pushed/merged.

## Court

Declared in README.md: Definition of Done = `just pre-commit`, chaining: fmt-check → check → lint → test-lib → coherence-check → guard-process-intelligence-boundary → guard-cheat-scan → guard-short-test-timeout → guard-fail-open-subprocess → guard-claims-schema → guard-pack-proofs → guard-generation-hash-pin → guard-pack-count → guard-gate-count → guard-pack-e2e-coverage → guard-ggen-toml-schema-parity → self-play. Cheapest gate first (fmt-check).

## Gate results at pinned head 03ceb0df6 (as-found)

| Gate | Exit | Finding |
|---|---|---|
| fmt-check | 1 | `crates/ggen-graph-wasm/src/{lib,oxigraph_engine,remote_engine}.rs` not rustfmt-clean (committed unformatted) |
| check (cargo, warm) | 0 | — (cold-cache first run hit the recipe's own 300s timeout, EXIT=124; warm 37.27s pass) |
| lint (clippy -D warnings) | 0 | 2m43s |
| test-lib | 0 | all crates green (e.g. 433 passed, 7 ignored) |
| coherence-check | 0 | O→A→O validation satisfied |
| guard-process-intelligence-boundary | 0 | — |
| guard-cheat-scan | 0 | (passes against the STALE stale file; re-fails after regeneration — see repairs) |
| guard-claims-schema | 1 | host env: system python3 = 3.9.6 has no stdlib `tomllib` (needs ≥3.11). Gate itself fine |
| guard-pack-proofs | 1 | `clap-noun-verb-pack` content hash mismatch vs `examples/receiptctl/ggen.lock` (FM-PACK-008), then FM-WRITE-005 clobber refusal; same class in `rmcp-pack` vs `examples/rmcp-verify` |
| guard-generation-hash-pin | 1 | gen:G5 pin `19c0d371…` absent from local clone (ledger is append-only; hash NOT fabricated) |
| guard-pack-count | 1 | rf:packCount "86" vs 94 real pack dirs |
| guard-gate-count | 0 | — |
| guard-pack-e2e-coverage | 127→1 | host: /bin/bash 3.2 lacks `mapfile`; under bash 5.3.3: 20 uncovered > baseline 19 (goat-capabilities-pack added post-baseline) |
| guard-ggen-toml-schema-parity | 0 | — |
| self-play | (via final run) | — |

## Repairs (all on fix/ggen-v26.9.17-boundary, atomic, generator-owned)

1. `0d2b4a9c9` — fmt: mechanical rustfmt on 3 ggen-graph-wasm sources. Guard: fmt-check → 0.
2. `4e2b4df3e` — pack/consumer reconciliation: `force: true` added to owning templates (`packs/clap-noun-verb-pack/templates/clap_noun_verb_routes.rs.tmpl`, `packs/rmcp-pack/templates/rmcp_stub_handler_proof.rs.tmpl`; precedent: chicago-tdd-tools-pack + crates/ggen-engine/src/write.rs:171-193 TECH-DEBT-003 note); projections regenerated BY `ggen sync run`; locks re-locked by deleting them and letting ggen re-lock (the error's own sanctioned remediation); ggen-cli-verify receipts refreshed by the guard run (actuator ggen@26.8.6→26.8.24, output hashes unchanged, chain re-signed).
3. `ebeb27515` — rf:packCount 86 → 94 in `.specify/repo-facts.ttl` (the guard's own named remediation; fact is hand-maintained by design).
4. `76a4d15b3` — pack e2e-coverage ratchet 19 → 20, conscious + named (goat-capabilities-pack), per the guard's own contract ("may never silently grow").
5. `5cc8808c1` — rmcp proof-strength restoration: the first full pre-commit re-run caught that force-regeneration had WEAKENED two tests (no assertions). Root cause: the committed strong proof was manufactured from an UNCOMMITTED pack state — the template facts were lost in a WIP split. rmcp is still 2.2.0 (the grounding version), so the facts hold. Reconstructed the template from its own witnessed projection; regenerated output is BYTE-IDENTICAL to the previously committed strong proof; guard-cheat-scan → 0 findings; guard-pack-proofs → 0.

Non-repo alternate (documented, no mutation): `guard-claims-schema` passes with python3.11 on PATH (`ALIVE: 19 claims validated`); `guard-pack-e2e-coverage` requires bash ≥4 (CI runs bash 5; host has 5.3.3 at /opt/homebrew/bin). `guard-generation-hash-pin` fixed WITHOUT repo change: `git fetch origin 19c0d371…` retrieved the real commit object from origin (the ledger was right; the clone was incomplete — exactly the guard's documented second cause). Append-only ledger untouched.

## Capability probe (cap-manufacture, read-only surface, no invented packs)

Observed live this session, against existing admitted packs only:

1. Render in memory: `ggen sync run --dry-run` in `examples/receiptctl` (6 wired packs) emitted the decision JSON — `written: [src/clap_noun_verb_routes.rs]`, 9+ `skipped: content identical` — proving resolve→enrich→extract→render executes against pack.toml → ontology.ttl → templates with SPARQL frontmatter.
2. Manufacture: `ggen sync run` produced the committed projection diffs (pack-driven comment-text change from 37767fd0c) and re-locked; re-sync `--dry-run` then reported `written: []` (idempotent fixed point).
3. Admission/validation gates fire for real: FM-PACK-008 (pack content hash vs ggen.lock) refused a stale pack; FM-WRITE-005 refused silent clobber of a differing projection (crates/ggen-engine/src/write.rs:73, :193); skip_if freeze marker honored (`ggen:local-override`).
4. Receipts: every consumer sync emits `.ggen-v2/receipt.json` (schema ggen-receipt/v2, BLAKE3 chain + signature); `guard-pack-proofs.sh:91-96` runs `ggen receipt verify` per consumer → "receipt valid+signed, all generated proofs pass" for all 6 consumers (`scripts/ci/guard-pack-proofs.sh:88, :106-110` idempotency snapshot diff).

Conclusion: cap-manufacture is real — packs manufacture artifacts deterministically, refuse unauthorized mutation, and sign what they did.

## Final court (post-repair head 5cc8808c1)

Full `just pre-commit` re-run in ONE invocation with conforming alternates on PATH (bash 5.3.3, python3.11.6):

- `PRE-COMMIT EXIT=0` — all 17 gates green in a single pass.
- Observed in log: `ALIVE: 13 generation entries hash-pinned` / `ALIVE: rf:packCount (94)` / `ALIVE: rf:gateCount (10)` / `ALIVE: no cheat patterns detected (1383 files)` (from the immediately preceding per-gate re-runs at this head) / self-play 7+3+2 passed / guard-pack-proofs "OK (6 consumers: sync clean, idempotent, receipt valid+signed, all generated proofs pass)" / zero `FAILED`/`BUILD_BROKEN` lines.
- Tree left clean at `5cc8808c12e465c89cf4e441a68cb340ca884ccc` (two ggen-cli-verify receipt files churn on every guard run; restored to committed state — the next guard run re-signs them identically-in-content).

Standing: **ALIVE** — with the pinned head BUILD_BROKEN as found; after 5 atomic repairs, cap-manufacture is observed manufacturing, refusing, and signing.

## Falsifiers attempted

- Tried sync WITHOUT force after pack change → refused (FM-WRITE-005): falsifies "sync silently clobbers"; refusal is the designed behavior.
- `git cat-file -t 19c0d371…` before fetch → absent locally; after fetch → `commit`: proves guard caught a clone gap, not a fabricated ledger hash.
- Byte-diff of regenerated rmcp proof vs witnessed original → first weaker, then (after template restoration) byte-identical: proves the template fully owns the artifact again.
- Idempotency probe (`written: []` on second dry-run) after every regeneration.

## 比 (ratio)

Every changed production byte is attributable to either `cargo fmt` (tool), `ggen sync run` (generator projection + lock + receipts), or a guard's own named one-fact remediation (rf:packCount, coverage baseline integer). Hand-written bytes: template reconstruction in 5cc8808c1 — template text transcribed from the generator's own previously witnessed output (reverse-sync to restore lost pack facts), 2 test bodies + comments. No consumer/repo source file was hand-edited. Operator keystrokes on 産面: zero files.

## Standing

**ALIVE** (boundary `ggen` / `cap-manufacture`).

- Pinned head as found: BUILD_BROKEN (fmt + pack/consumer drift + facts drift + clone gap).
- Final head `5cc8808c1` (5 commits on fix/ggen-v26.9.17-boundary): full DoD court exit 0; manufacture capability witnessed live (render → write → idempotent fixed point → BLAKE3-signed receipts; pack admission hash-gate and clobber-refusal both fired).
- Residual, lawful, named: (1) cold-cache `cargo check --workspace` exceeds the recipe's own 300s timeout on this host (warm: 37s, green); (2) two guards require host runtimes CI provides by default (bash ≥4, python ≥3.11) — alternates demonstrated; (3) pack e2e-coverage gap 20/94 consciously baselined (goat-capabilities-pack named); (4) ggen-cli-verify receipt files churn per guard run by design (append-only log).
- Not done (out of scope, no authority): no push/PR/merge; marketplace untouched (read-only, never modified); no new packs admitted.

## What the operator did NOT have to write

Every production byte in the 5 commits: rustfmt (tool), `ggen sync run` (generator: 2 projections, 2 locks, 2 receipt refreshes), or a guard's own named one-fact/one-integer remediation. The only authored text was template reconstruction in 5cc8808c1, transcribed from the generator's own previously witnessed output. Operator wrote zero repo files.
