# Boundary receipt — beam4pm / cap-process-court (r8) — v26.9.17 qualification wave

Task: qualify-boundary(beam4pm, cap-process-court) — observe-process / OCEL-style process mining on the BEAM; process-conformance; xaas posts OCEL events to an ex4pm_web-class consumer.

## STANDING: ALIVE

## Pin
- Repo: /Users/sac/beam4pm, branch main, HEAD = ace23e584e89ecc52775b9155ef9592df67d90a3 (re-pinned via `git rev-parse HEAD`; matches assigned ace23e5; unchanged across the whole session).
- Working tree: 17 dirty entries at start (11 modified, 6 untracked) — inventoried, never stashed/discarded, verified WITH dirt. Dirt delta by session end: +5 `research/erc/ERC-002-*.json` — machine-written receipts emitted by the e2e conformance test itself (same pattern as the 3 pre-existing). Zero source files modified by this qualification.
- Dirt vs court: NOT court-breaking. All dirty files are additive (new dfcm planner module+test+fixtures, refreshed capture event_ids/timestamps, additive engine tests). The one compile warning (lib/beam4pm_dfcm.ex:179, `AshAutofde.CascadeAllocator.allocate/3` undefined) is a guarded optional dependency by design (`Code.ensure_loaded?` gate → named fallback `{:error, :allocator_unavailable}`).

## Environment / court
- OTP 27 (erts-15.2.2), Elixir 1.18.4, rebar3, gleam, cargo (wasm32-wasip1 installed).
- Court (Makefile/Justfile `test`): `rebar3 eunit` + `mix test`. All-local: Bandit listeners 127.0.0.1:4210/4211 booted only inside the test VM (lib/beam4pm_application.ex). No external services required.
- Engine substrate: rust4pm (`process_mining =0.6.2`) is THE one process-mining engine, compiled to wasm32-wasip1, hosted by wasmex; host only packs/unpacks JSON (lib/beam4pm_rust4pm.ex:1-96).

## Capability evidence (file:line)
1. observe-process (OCEL on BEAM):
   - Admitted OCEL record types in ontology.ttl (ocel_event, ocel_object, ocel_relationship, event_log, ...) with validating constructors (README "Current status"; BeamPM.Types.Manifest).
   - BeamPM.Discovery.traces_from_events/2 — lib/beam4pm_discovery.ex:63-86; dfg_from_traces/1 — :93-107.
   - Rust4PM OCEL 2.0 ops import_ocel_json/xml — lib/beam4pm_rust4pm.ex:72-74; ocel_new/ocel_add_event_type/ocel_add_object/ocel_add_event exercised live in the e2e test.
2. process-conformant (conformance):
   - BeamPM.Discovery.conformance/2 — lib/beam4pm_discovery.ex:151.
   - BeamPM.PowlConformance.check_conformance/3 — lib/beam4pm_powl_conformance.ex:74: real Alpha+++ Petri-net discovery + align_trace + compute_fitness over the SAME flattened OCEL variant traces; disclosed real limitation (no POWL→Petri op) in moduledoc.
   - Real ETC precision: beam4pm_precision:etc_precision/2,3 (src/beam4pm_precision.erl), called from src/beam4pm_discovery.erl:243; Gleam leg gleam/src/beam4pm/discovery.gleam:149.
3. OCEL consumer (ex4pm_web-equivalent, re-homed):
   - BeamPM.OcelIngest.Router — lib/beam4pm_ocel_ingest.ex:2; POST /ocel/events :31, POST /ocel/objects :36; generated by ggen_igniter from the bpmi:AdmittedIngestRoute admission graph in ontology.ttl; unadmitted route → 404 route_not_admitted :41.
   - scripts/ocel_ingest_sync.sh: "beam4pm's own independent OCEL v2 network ingestion layer — the manufactured equivalent of ex4pm's retired ex4pm_stream/ex4pm_web umbrella apps. NOT a dependency on ex4pm."
   - Consumer suite: test/beam4pm_ocel_ingest_test.exs (real Plug.Test conns; 201 admits, 404 unadmitted, constructor-typed refusals).
4. E2E conformance loop (deviation detection): test/beam4pm_powl_conformance_e2e_test.exs:28-29 — fixtures qualification/gym_bridge/{reference,deviant}_ocel_events.json (captured from ash_a2a's real HDDL→A2A→HTTP POST run). Non-vacuous assertions: 18 ref / 5 deviant events; deviant truly omits clean_house; `refute result.conforms`; alignment cost > 0; deviation set contains `[">>", "clean_house"]`; fitness < 1.0.
5. Contract anchor: `observe_process_twin` — contracts/post-llm-runtime/beam4pm_pro.ttl:7 (b4:pro_runtime rt:enablesCapability).

## Commands + exits (all observed this session)
| Command | Result |
|---|---|
| `git rev-parse HEAD` | ace23e584e89… (twice; unchanged) |
| `mix compile` | exit 0 (1 guarded-by-design warning in untracked dirt) |
| `bash scripts/rust4pm_wasm_build.sh` | exit 0; rust4pm_wasm.wasm built, sha256 6a66c0f1…, 1,293,971 B |
| `cargo build --release` × native/{rf1-dfg,rf2-conformance,rf3-ocel,rf4-oc-discovery}-oracle | exit 0 ×4 (36.7s/22.7s/48.6s/24.9s) |
| targeted process court: `mix test test/beam4pm_discovery_test.exs test/beam4pm_powl_conformance_test.exs test/beam4pm_powl_conformance_e2e_test.exs test/beam4pm_ocel_ingest_test.exs` | 41 tests, 0 failures, exit 0; ERC-002 receipt emitted |
| `rebar3 eunit` | All 3100 tests passed, exit 0 |
| `mix test` (bare) | exit 2: 1108 tests, 22 failures, 1 invalid, 88 skipped |
| `mix test test/beam4pm_rf2_conformance_test.exs test/beam4pm_rf3_ocel_test.exs` with env contract | 11 tests, 0 failures |
| `mix test` (with RF2_/RF3_ env contract) | **exit 0: 1108 tests, 0 failures, 88 skipped** |

Env contract (all values repo-defined; fixtures checked in):
```
RF2_ORACLE_BIN=native/rf2-conformance-oracle/target/release/rf2-conformance-oracle
RF2_CLEAN_XES=qualification/fixtures/receipt.xes
RF2_MUTATED_XES=qualification/fixtures/mutated_receipt.xes
RF3_ORACLE_BIN=native/rf3-ocel-oracle/target/release/rf3-ocel-oracle
RF3_{POSITIVE,N13,N14,N05}_FIXTURE=qualification/fixtures/{positive-self-authored,n13-duplicate-object-id,n14-undeclared-event-type,n05-o2o-dangling}.ocel.json
```

## Classification of the intermediate 22 failures (root-caused, then cleared)
All 23 red items shared environmental root causes, not code defects:
1. Unbuilt native differential-oracle binaries (rf1/rf2/rf3/rf4) — each test's own error message names the build command; cleared by building them.
2. Unset `RF2_ORACLE_BIN`/`RF2_CLEAN_XES`/`RF2_MUTATED_XES`/`RF3_*` env contract — repo docs classify as "pre-existing/environmental" (docs/jira/v26.9.11/powl-plan-execute-conform-loop.md:81); cleared by wiring to checked-in fixtures.
The 88 remaining skips are the repo's own named artifact skips (petgraph/tract/ferroplan wasm engines + gleam facade unbuilt; e.g. `BeamPM.Petgraph.wasm_missing_reason()` — "run scripts/petgraph_wasm_build.sh first"). These engines are OUTSIDE the r8 process-court capability; the one process-mining engine (rust4pm) was built and is exercised throughout.

## Repairs / alternates
- No code repairs; no fix branch created (nothing to fix on the tree — all greens achieved via repo-named provisioning + env wiring). No push/PR/merge (per constraint).
- Lawful alternates used: repo's own build scripts (rust4pm_wasm_build.sh, cargo per oracle crate) and the repo's own env-var contract instead of declaring blocked.

## Falsifiers attempted
1. "Conformance could be vacuous" → falsified: e2e asserts named deviation `[">>", "clean_house"]` with cost > 0, fitness < 1.0 against a captured deviant trace; the test module encodes its own falsifier in the ERC-002 receipt.
2. "Capability might be emulated host-side" → falsified: single engine (rust4pm in wasm); host is a JSON pack/unpack shim; differential court proves wasm engine ≡ native oracles byte-for-byte (RF1 T1/T11, RF2, RF4 tests — now passing).
3. "Dirty fixtures might be hand-forged" → falsified: diff is event_id/timestamp refresh only, structure identical, consistent with docs/jira/v26.9.11/e2e-reverification-artifacts.md; e2e passes against them.
4. "Dirt might break the court" → falsified: full court green with dirt present; dirt is additive.
5. "Skips might hide the capability" → falsified: the 88 skips are all named non-process engines (petgraph/tract/ferroplan/gleam); zero process-court tests skip (wasm built).

## 比 / what the operator did NOT have to write
- Hand-written production lines this session: 0. Receipts written: this file only (/tmp, evidence not product).
- Operator did NOT write: the wasm build, 4 oracle builds, any test, any fixture, any env scaffolding (all repo-defined), any repair, any OCEL/conformance code (all ggen/ggen_igniter-manufactured or previously manufactured), and 5 test-emitted ERC-002 receipts.
- Ledger deltas: none (no 産面 hand-edits). Standing deltas: cap-process-court UNKNOWN → ALIVE.

## Session summary line
repo=beam4pm base=main@ace23e5 commands=9 obs exits: compile=0 wasm_build=0 oracle_build=0x4 targeted=0(41t) eunit=0(3100t) mixtest=2→0(after provisioning, 1108t/88 named skips) repairs=none(provisioning only) falsifiers=5 all survived standing=ALIVE
