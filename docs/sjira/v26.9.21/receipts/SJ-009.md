# SJ-009 Receipt: ash_ai dependency retest against current deps

- **Verdict**: COMPATIBLE (no failing dependency edge observed)
- **Standing**: ALIVE for the retest subject (`mix deps.get && mix compile && mix test` observed passing on this exact tree)
- **Date**: 2026-09-21
- **Replay identity**: `sjira-v26.9.21-sj-009`
- **Repository**: seanchatmangpt/xaas, branch `sjira/sj-009`, worktree `/Users/sac/xaas-worktrees/sjira/sj-009`
- **Subject SHA (tree under test)**: `01aa6fbf0b8fc5430f6a48bc4ccd24e11d4ac30a` (order `base_sha` `8e72cfc` is an ancestor)
- **Receipt classes**: manufacture, verification
- **Workers**: zcode-cli 0.16.5 default agent (no `--agent`), then Claude fallback (see Workers)

## Result

`ash_ai "~> 1.0"` (locked 1.0.3) and `req_llm "~> 1.18"` (locked 1.20.0) are already present in
`mix.exs` / `mix.lock` at this base and are load-bearing in `lib/` and `test/` (MCP surface, Library
`prompt/2` generic action, Groq adapter). The earlier probe incompatibility (0.8.2-era finch/req_llm
conflict, tag `archive/probe-ash-ai-dependency-retest-fa744ec`) does **not** reproduce against the
resolved set:

| Package | Locked |
|---|---|
| ash | 3.33.9 |
| ash_ai | 1.0.3 |
| ash_postgres | 2.13.1 |
| finch | 0.23.0 |
| req | 0.7.4 |
| req_llm | 1.20.0 |

`mix.exs` sha256 `6e4fca69406d73cccb942cef104af05f166eddcefd5f97017ec62501d8c24b19`,
`mix.lock` sha256 `9d8343c16444361b29cc422fc3d70f659d824767bc47229ddacf4fb407f3161b`.
Neither file changed (`git diff 01aa6fbf -- mix.exs mix.lock` is empty); `deps.get` re-resolved to the
same lock. ash_ai is intentionally left in `mix.exs`: it is already on main and used by
`lib/xaas_web/mcp_scope.ex`, `lib/xaas/library/book.ex`, `lib/xaas/library/explainer/groq_adapter.ex`
and others, so removing it would break the app rather than restore any earlier state.

## Commands and exits (real, this worktree)

| # | Command | Exit | Observed |
|---|---|---|---|
| 1 | `mix deps.get` | 0 | `All dependencies have been fetched`; ash_ai 1.0.3, finch 0.23.0, req_llm 1.20.0 resolved |
| 2 | `mix compile` | 0 | `Compiling 478 files (.ex)`, `Generated xaas app`; one warning, from `deps/ex4pm` (dep code, not xaas) |
| 3 | `mix test` (`MIX_TEST_PARTITION=_sj009`) | 1 | `ecto.create` for `Xaas.LegacyRepo` timed out under host load (load avg ~560); no tests ran |
| 4 | `mix test` | 2 | `1346 tests, 34 failures (71 excluded)`, all 34 `DBConnection.ConnectionError` (pool checkout starved) |
| 5 | `mix test --failed --max-cases 2` | 1 | boot failed: Postgres `FATAL 53300 too_many_connections` (sibling workers on the shared server, `max_connections=200`) |
| 6 | `mix test --failed --max-cases 2` (after headroom returned) | 0 | `34 tests, 0 failures` |
| 7 | `mix test` (full, exact tree) | 0 | `1346 tests, 0 failures (71 excluded)`, `Finished in 319.5 seconds` |

Runs 3 to 5 are preserved, not hidden. Classification of the failures: 34 of 34 are
`DBConnection.ConnectionError` on `Xaas.Repo` (`connection not available and request was dropped from
queue`), none reference ash_ai, req_llm, Finch, Req or a `FunctionClauseError`; cause is Postgres
connection exhaustion from concurrent sibling worktree test runs on the one shared server. Falsified as a
dependency problem by runs 6 and 7 (identical tree, connection headroom present: 0 failures). All
failures were introduced by the environment this session, none by the dependency set; there is no
pre-existing xaas failure in the passing run.

## Courts

| Court | Result |
|---|---|
| `compile` | PASS (run 2) |
| `tests` | PASS (run 7: 1346 tests, 0 failures) |
| `chicago_no_mocks` | PASS. `grep -rn "Mox\b\|:meck\|unittest.mock\|MagicMock\|monkeypatch" test/ lib/` hits only comments/docs (`test/xaas/generation_test.exs:7`, `test/xaas/library/ils_repo/sip2_adapter_test.exs:5`), the self-referential verify-task fixtures (`test/mix/tasks/xaas_verify_and_commit_test.exs`) and the verify task's own grep pattern (`lib/mix/tasks/xaas.verify_and_commit.ex:140`). Changed lib/test files in this branch: 0. |

## SA2A usage (real autofde beam-bridge, `Xaas.Sa2a.Bridge`)

- **Admission** (before construction): `receipts/sa2a-admit-SJ-009.json`, receipt `rec-acf0465d`,
  `ok: true`, reason `CONFORMS_TO_SPEC`, standing `KNOWN`.
- **Replay** (after verification): `receipts/SJ-009.verification-manifest.json` (commands, exits, test
  counts, lock hashes) was replayed through `docs/sjira/v26.9.21/sa2a_loop.exs replay`; result in
  `receipts/sa2a-replay-SJ-009.json`: `computed_hash == expected_hash ==
  63e41d1852e5720710f2181d86ec272667dd532f4206f53328e8218d22ff1eed`, `verified: true`. Falsifier: the
  same manifest against an all-zero hash returned `verified: false`.
- `sa2a_execute` (the DO edge) was not called; SA2A here validates, admits and replays only.

## Workers

- **zcode default agent** (`node /Users/sac/dev/zcode-cli/bin/zcode.js --cwd . --mode edit --json`, no
  `--agent`). Attempt 1 exited non-zero with an argument-parse error (the prompt starts with `---`;
  log kept at `sj-009.zcode.attempt1-argerror.log`); attempt 2 used `--prompt=<text>`, exit 0, session
  `sess_74e051b0-dea1-4048-a00e-aac0f6a95ac6`. It confirmed read-only that ash_ai/req_llm are already
  on main and load-bearing, ran the mock-grep court, and drafted a receipt, but reported BLOCKED: its
  headless permission mode (`build`, no approval client) refused every `mix` and `git add/commit`
  call (`No permission client configured for Bash`). It made no source edits and produced no
  commit; its receipt text was an untracked draft and is superseded by this file.
- **Claude fallback** (this supervisor session) ran `deps.get`, `compile`, `test`, the failure
  classification, the failed-set rerun, the SA2A replay, and wrote this receipt. worker_kind is
  therefore `mixed`: zcode contributed the dependency-state and mock-court findings, Claude
  contributed all execution.

## Falsifiers

- "result reported without running deps.get + compile + test": not triggered; all three run, exits above.
- Dependency incompatibility reproducing: not observed; a full `mix test` passed with ash_ai 1.0.3 / req_llm 1.20.0 / finch 0.23.0 / req 0.7.4.
- Would flip the verdict: a failure whose stack enters `AshAi.*`, `ReqLLM.*`, `Finch` or `Req.Finch.finch_name/1` on this tree.

## Bounds

- Runs 4 and 7 used a partitioned test database (`xaas_test_sj009`) on the shared local Postgres; the
  runnable check in the order names no partition. Test tags excluded by default (`:stress`, `:kind`,
  `:external`, `:external_llm`, and others, 71 tests) were not run.
- Run 7 is the exact-tree full pass; the deps.get, compile and test steps ran as three commands on the
  same tree rather than one `&&` chain, with `mix.lock` unchanged between them.
