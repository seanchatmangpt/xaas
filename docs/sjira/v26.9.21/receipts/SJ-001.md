# SJ-001 receipt: xaas consumes a real Semantic Jira WorkOrder end to end

Standing: ALIVE for the exact subject `xaas-semantic-jira-e2e` (admit, materialize, receipt,
replay). Observed on code head `41e28e5` (branch `sjira/sj-001`, base
`01aa6fbf0b8fc5430f6a48bc4ccd24e11d4ac30a`). The commit that adds this file changes no other
path.

## Subject and authority

- Order: `docs/sjira/v26.9.21/001-xaas-semantic-jira-e2e.md` (front matter untouched).
- Admitted digest: `sha256:4e0458934ec42dcc4953107d1fde191cc2ab2cb27f33fcf0804a30bfa6b91dda`
  (real `GgenIgniter.SemanticJira.admit_work_order/1`, nested `mix run` in `~/ggen_igniter`).
- Authority: NONE. No merge, push or publish was performed.

## Construction (who did what)

| step | actor | result |
|---|---|---|
| construct | zcode-cli 0.16.5 default agent (no `--agent`), `--mode edit --json` | exit 0, file-tool edits only; every Bash call refused (`No permission client configured for Bash`), so nothing it wrote was executed. The first invocation did no work (its option parser reported `--prompt` as ambiguous because the value starts with `---`); the run that produced the edits used `--prompt=<text>`. Note preserved in `sj-001/zcode-headless-execution-refusal.md`. |
| verify and repair | supervising Claude session | fixed `{:ok, wo} = Jason.decode!()` MatchError in `e2e_project.exs`, 2-tuple vs 3-tuple `project/3` matches, single-court suite replaced by the three required courts, asdf-shim toolchain replaced by the PATH toolchain plus a private `_build` clone, moduledoc indentation regression, added `--help` to both mix tasks and tests |

## Definition of done (observed, exit codes)

Runnable check, exact: `mix test test/xaas/ultracode && mix xaas.semantic.materialize --help`
(`MIX_TEST_PARTITION=sj001`): `535 tests, 0 failures (21 excluded)`, exit 0; the `--help`
command printed the task usage, exit 0. Also on head: `mix compile --warnings-as-errors` exit 0
in `dev` and `test`; `mix format --check-formatted` on all changed files exit 0; mock grep
(`Mox|:meck|unittest.mock|MagicMock|monkeypatch|Mock(|patch(`) over the changed tests and
sources: no matches.

1. Admit: `evidence/admit.json`, digest above; the SA2A admission of the same assertion
   (`rec-68ad2c76` earlier, `rec-a56963e3` re-run, `sj-001/sa2a-admit-rec-a56963e3.json`) has an
   identical `candidate_hash` `e1b25b3b60c51c61f5f5a63bf54311e5568742619890056e0634d13c03c6000f`.
2. Materialize and receipt: real `Mix.Tasks.Xaas.Semantic.Materialize` and
   `Mix.Tasks.Xaas.Semantic.Receipt` in `test/xaas/ultracode/semantic_jira_e2e_test.exs`.
   The worktree HEAD equalled the order's `base_sha`; the fabric verifier ran the three required
   courts (`compile`, `tests`, `chicago_no_mocks`) as shell steps at the candidate head
   `09e2e0d3021023a0e25d7066ccd2dd1361750795`, all `pass`, outcome `alive`, `head_verified` true
   (`evidence/receipt.json`).
3. Replay: sealed digest `sha256:b673dee909033a35a8672e4e1b8b5fb11a46775eea54c85df839599472c9a1ed`
   reproduces from the file on disk and from a DB re-export (`evidence/replay.json`).
   SA2A replay of the receipt manifest through the real autofde bridge (`sa2a_loop.exs replay`):
   `verified: true`, computed hash `3099a9a20590a6f6a297b95fb64fc1ae1dd724c20bf5a4b7bccaa499bec6e46c`;
   the all-zero wrong hash is refused (`sj-001/sa2a-replay.txt`).

## Falsifiers

- Digest altered after admission is accepted by materialize: refuted. `SemanticWork.admit/1`
  now refuses a descriptor whose optional `admission_digest` differs from `graph_digest`
  (`{:refused_semantic_work, {:admission_digest_mismatch, _}}`, malformed shapes refused);
  the graph-side emitter guard in `e2e_project.exs` refuses a stale `EXPECTED_DIGEST`. Both are
  asserted in the e2e test and in `semantic_work_test.exs`.
- Receipt seals without the required courts passing: refuted. The control candidate (no worker
  note) fails the `tests` court and seals `build_broken` although the worker claimed `alive`
  (`evidence/control.json`, digest `sha256:0cbebc734aeba6e83788ca09bbdd5ff8603c039cbfb20c662272c2d73705cd8e`).

## Scope of the claim

- The graph side ran `GgenIgniter.SemanticJira.admit_work_order/1` and `definition_digest/1`;
  the `mix semantic_jira.*` tasks and `Descriptor.receipt_from_xaas/2` are not present in the
  `~/ggen_igniter` checkout used (branch `feat/zcode-ocel-pack`), so the graph-side receipt
  reconciliation edge is UNSUPPORTED here and was not exercised. The descriptor in this proof is
  projected by `docs/sjira/v26.9.21/e2e_project.exs`, not by a `semantic_jira.descriptor` task.
- The worker is a scripted lease-protocol client; it stands in for model judgment only.
- The e2e test skips visibly when the graph checkout is absent.
- The three courts are fixture shell steps over the exact-head worktree, not the repository's
  own `mix compile` and `mix test`.
- Load note: the machine ran at load average 300 to 550 during verification. The first two e2e
  attempts hit the graph process's alarm (exit 142) while it went through the asdf shim; the
  attempt with the PATH toolchain and a private `_build` clone passed in 59 s. The cause is
  attributed to a cold graph-tree recompile under the shim toolchain, not separately measured.
  The shared `~/ggen_igniter/_build/test` was rebuilt with the PATH toolchain afterwards
  (`mix run` exit 0).
