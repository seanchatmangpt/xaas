# SJ-003 manufacturing receipt

Order: `docs/sjira/v26.9.21/003-handwritten-paydown-zcode-plugin.md`
Repo: seanchatmangpt/xaas. Base `01aa6fbf0b8fc5430f6a48bc4ccd24e11d4ac30a`.
Branch `sjira/sj-003`. Code head `191e7d59c65b78bf35459ffcc6c16bee10bd8076`
(the docs commit that adds this file changes no code). Not merged, not pushed.

**Standing: PARTIAL_ALIVE.** Every acceptance criterion was observed on the
code head. The order's literal runnable check
`ggen sync && git diff --exit-code && mix test` was not observed as one passing
chain: its root-level `ggen sync` did not complete (see Blocked).

## Worker

`worker_kind = mixed`. The zcode default agent (`zcode 0.16.5`, no `--agent`,
`--mode edit --json`) ran first and produced no product edits: under `--mode
edit` every non-builtin Bash command was refused (`No permission client
configured for Bash`), and the provider returned 25 rate-limit refusals
(`[1302] Rate limit reached`, HTTP 429). The supervisor stopped it after 11+
minutes. Its own record is `receipts/SJ-003-zcode-attempt.md`; log
`~/xaas-worktrees/sjira/sj-003.zcode.log`. A first invocation with the
prescribed `--prompt "$(cat ...)"` form failed at argument parsing (the prompt
starts with `---`; `Option '--prompt' argument is ambiguous`, log
`sj-003.zcode.attempt1.log`); it was re-run as `--prompt=...`. The construction
below was done by the supervisor in the same worktree.

## Construction (mu)

Generated versus hand-written:

| Artifact | Kind |
|---|---|
| `priv/zcode_plugin/marketplace/**` (9 files) | generated by `ggen sync run`, byte-identical to the previous checked-in projection |
| `priv/zcode_plugin/packs/zcode-plugin-pack/{pack.toml,ontology.ttl}` | hand-written pack vocabulary |
| `packs/zcode-plugin-pack/templates/*.tmpl` (8) | moved from `templates/`; hooks/manifest templates verbatim; `script-xaas-gate.mjs.tmpl` re-parameterized so its constants are SPARQL-driven |
| `packs/ultracode-actuation-lease-pack/{pack.toml,ontology.ttl,templates/script-lease.mjs.tmpl}` | hand-written pack; template parameterized by `ul:LeaseStateProtocol` |
| `priv/zcode_plugin/ontology.ttl` | gate policy lifted into individuals (`zp:AllowedTool`, `zp:ToolMapping`, `zp:AllowedBashVerb`, `zp:ForbiddenGitFlag`, `zp:DeniedPath`) and `ul:LeaseStateProtocol` |
| `priv/zcode_plugin/ggen.toml`, `ggen.lock` | `[packs]` path bindings; lock is generated by ggen and pins both pack hashes |
| `test/xaas/zcode_plugin/projection_test.exs` | one-line change: the temp project now also copies `ggen.lock` and `packs/` (outside `path_scope`; necessary because the drift test builds its own project copy) |
| `HANDWRITTEN.md` | two rows removed, one row retargeted, paydown notes added |

Residual hand-authored logic: the gate's enforcement code (tokenizer, decision
function) lives inside the pack template, not as consumer residue. The prose
templates (command, agent, skill) moved into the pack but remain hand-edited,
so their row stays Active.

## Commands and exits (worktree; tree content identical to the code head, and the two whole-suite runs are on the head itself)

| Command | Exit | Observation |
|---|---|---|
| `cd priv/zcode_plugin && ggen sync run` | 0 | 9 outputs, all `skipped: unchanged: content identical` |
| clean-room: copy `ggen.toml ggen.lock ontology.ttl templates packs` to a temp dir, `ggen sync run`, `diff -r marketplace` | 0 / empty diff | rendered projection identical to checked-in |
| negative control: change `.npmrc` -> `.npmrc-MUTATED` and `ul:conflictExitCode 65` -> `77` in a temp copy, re-sync | 0 | `xaas-gate.mjs` line 45 and `xaas-lease.mjs` lines 11 and 73 change, so the scripts really derive from the ontology |
| `git diff --exit-code -- priv/zcode_plugin HANDWRITTEN.md test lib` after re-sync | 0 | rendered output does not drift |
| `mix compile` | 0 | |
| `mix test test/xaas/zcode_plugin/` | 0 | 33 tests, 0 failures (includes real-`ggen` drift test, gate and lease-script tests against the real controller) |
| `mix test` (run 1, `--max-cases` default 32) | 2 | 1346 tests, 84 failures; all 84 are `DBConnection.ConnectionError` / Postgres `53300 too_many_connections` (shared server at 200/200 from sibling runs). Not related to this change |
| `mix test` on the 27 failing files, `--max-cases 2` | 0 | 205 tests, 0 failures |
| `mix test --max-cases 4` (run 2, whole suite) | 0 | 1346 tests, 0 failures, 71 excluded by default tags |
| `grep -rn "Mox\b\|:meck\|unittest.mock\|MagicMock\|monkeypatch\|Mock(\|patch(" test/xaas/zcode_plugin/` | 1 (no match) | Chicago-style; changed test file has no doubles |

The `MIX_TEST_PARTITION=sj003` database partition was used for every test run.

HANDWRITTEN.md Active rows: 20 before, 18 after (removed: gate script row,
lease script row).

## Blocked / unverified

- Root-level `ggen sync run` (the runnable check's first step) was started and
  ran 36+ minutes: it iterates one `mix ash.gen.resource` per resource (88 receipts,
  each "No proposed content changes!") and then `mix ash.extend` calls. It never
  finished; the supervisor killed it. It does not render `priv/zcode_plugin`.
  Its side effects were untracked codegen files (a migration and resource
  snapshots outside `path_scope`), which were deleted before commit. Status of
  that step: UNKNOWN.
- Not done, outside this order's `path_scope`: publishing the two packs to
  `ggen-marketplace` (they are repo-local path packs). `docs/ultracode/FAILOVER-RUNBOOK.md`
  and the moduledocs of `gate_test.exs`, `lease_script_test.exs` and
  `worker_doctrine_test.exs` still mention `priv/zcode_plugin/templates/`.
- Rows for `lease.ex`, the execution-fabric controller and `xaas.receipts.ex`
  were not removed: those files are still hand-written Elixir and no pack
  renders them (removal would trip the order's second falsifier).
- Editing a pack now requires deleting `priv/zcode_plugin/ggen.lock` so ggen
  re-locks (`FM-PACK-008` otherwise); the drift test copies the lock, so an
  unlocked pack edit fails the test.

## SA2A (real autofde beam-bridge)

- Order admitted before construction: `rec-e088bcd4` (`receipts/sa2a-admit-SJ-003.json`).
- After construction (`receipts/sa2a-post-SJ-003.json`, script
  `receipts/sa2a_post_SJ-003.exs`, manifest `receipts/SJ-003-manifest.json`):
  - `admit` of `workorder:SJ-003 digest:sha256:ecbcd238... constructed head:191e7d5...`
    returned `rec-ffcfcbb6`, `CONFORMS_TO_SPEC`, standing `KNOWN`.
  - `replay` of the construction manifest against its canonical sha256
    `e5d31200ff7bedad1879d27575cf02743b35fe17ff14bb2d8a1acea8cdce6085`:
    `verified: true`.
  - Wrong-hash control (`000...0`): `verified: false`.
  - `sa2a_execute` was never called. SA2A admission and replay are candidate-shape
    and integrity checks; they grant neither authority nor `ALIVE`.

## Falsifiers

1. A rendered file differs from the checked-in one: clean-room render diff was
   empty. Not triggered.
2. A HANDWRITTEN.md row removed while the file is still hand-edited: the two
   removed rows name the gate and lease script templates whose stated missing
   capability (constants as ontology individuals; a pack that renders the lease
   protocol) is now supplied. The prose templates, whose text is still
   hand-edited, kept their row. Not triggered, with the residual noted above.
