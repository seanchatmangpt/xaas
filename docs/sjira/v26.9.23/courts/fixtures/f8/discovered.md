# Discovered improvement (GC23-2 F8 fixture)

Fixture prose for the F8 check of the GC23-2 court (ARD section 26 F8, PRD
PR-017; lane V23-K). It is not accepted prose of GC-26.9.23: it stands for an
improvement discovered after the GC23 propositions were admitted, which
falsifies none of them.

The stop court also writes its per-gate table as one JSON document next to the text table.
