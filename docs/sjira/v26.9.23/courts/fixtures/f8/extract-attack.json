[
  {
    "kind": "Postcondition",
    "statement": "The stop court writes its per-gate table as one JSON document next to the text table.",
    "quote": "The stop court also writes its per-gate table as one JSON document next to the text table.",
    "required_by": "GC23-8",
    "boundary_class": "LastMile"
  }
]
