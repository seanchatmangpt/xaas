# Stream sj001-guard

Branch `errc2/sj001-guard`, worktree `/Users/sac/xaas-worktrees/errc/sj001-guard`, base
`2ec5fdb87ef1e144a1659628be148e3105efbd40`, merged with `sjira/sj-001` (`b64f17b`, `--no-ff`,
no conflicts). Order: `docs/sjira/v26.9.21/001-xaas-semantic-jira-e2e.md`. Receipt:
`docs/sjira/v26.9.21/receipts/SJ-001.md`.

## Charter (Define)

Close the verifier defects recorded for SJ-001 wave 1: falsifier 1 reproducibly triggered
(digest altered after admission accepted when the envelope is omitted or altered consistently),
no tests for those probes, fixture-only courts, order and receipt out of sync with what was
observed.

## Baseline (Measure)

- Pre-fix probe on the real descriptor (pre-fix module compiled from `sjira/sj-001`):
  untampered ACCEPTED, A refused, B ACCEPTED, C ACCEPTED.
- The real emitter (`origin/feat/semantic-jira-descriptor-bridge`, `Descriptor.build/4`) sends
  `graph_digest` = digest over every definition digest of the graph, and `bridge.source_snapshot_digest`
  = the per-work-order digest. The recorded real crown descriptors confirm the two differ.
  So "graph_digest must equal source_snapshot_digest" (the verifier's suggestion) would refuse
  every real descriptor and was rejected on that evidence.
- `ggen_igniter 26.9.12` (hex, what xaas resolves) has no `SemanticJira`; xaas cannot call the
  graph library to recompute.

## Alternatives (Explore)

1. Require the envelope: still a second in-band copy; probe C defeats it. Rejected.
2. Bind `graph_digest` to `bridge.source_snapshot_digest` unconditionally: breaks the real
   emitter. Rejected (falsifier: crown descriptors, see above).
3. Recompute the graph-wide `graph_digest` in XaaS: needs the whole graph's admitted work
   orders and the graph's admission normalization; duplicates graph semantics. Not built.
4. Carry the admitted snapshot; XaaS recomputes `work_order_digest` / `definition_digest`
   from content, cross-checks every digest copy, and the trusted caller declares whether
   `graph_digest` is per-work-order (`:snapshot`) or graph-wide (`:graph`). Built.

## Construction (Develop)

- `lib/xaas/ultracode/semantic_work/admission_binding.ex` (new, handwritten residue, recorded in
  `HANDWRITTEN.md`).
- `lib/xaas/ultracode/semantic_work.ex`: `admit/2`, `materialize/2` pass `binding:`; new optional
  key `admitted_work_order`; contract docs.
- `lib/mix/tasks/xaas.semantic.materialize.ex`: `--binding auto|snapshot|graph` (whitelisted).
- `docs/sjira/v26.9.21/e2e_project.exs`: the projection also emits `admitted_work_order`.
- `test/xaas/ultracode/semantic_work_test.exs`: 13 tests (probes A, B, C, stripped anchors,
  edited and re-sealed snapshot, descriptor fields, graph mode, no-anchor descriptors, malformed
  anchors and options, materialize refusal); fixture is the committed real descriptor.
- `test/xaas/ultracode/semantic_jira_e2e_test.exs`: real `mix compile` court, ten materialize
  probes, a candidate that fails the `tests` court and one that does not compile.
- Evidence regenerated by the e2e test through `SJ001_EVIDENCE_DIR`; SA2A replay re-run for the
  new sealed receipt.

## Generated versus handwritten

All source above is handwritten. The evidence JSON is emitted by the real processes (graph
admission, mix tasks, fabric receipt export). No generated output was hand-edited. zcode was not
invoked in this stream (`claude_direct`).

## Commands and results (Implement)

Partition `sj001g`, one `mix test` at a time.

- `mix test test/xaas/ultracode/semantic_work_test.exs`: 27 tests, 0 failures.
- `mix test <semantic_work_test> <semantic_jira_e2e_test> <semantic_tasks_help_test>`: 31 tests,
  0 failures, exit 0, 97.5 s.
- `mix test test/xaas/ultracode`: 548 tests, 0 failures (21 excluded), exit 0, 152.8 s.
- `MIX_ENV=test mix compile --force --warnings-as-errors`: exit 0.
- `mix format --check-formatted` (whole project): exit 0.

Pre-existing versus introduced: no failure occurred in these runs, so there is none to
attribute. The single compile warning is in `deps/ex4pm/mix.exs` (a dependency), not in this
tree's sources.

## Unresolved (branches left open)

- UNSUPPORTED (graph side): the real emitter does not send `admitted_work_order`; until it does,
  the default `auto` binding cannot bind a real graph-wide `graph_digest`. Needs a change in
  `~/ggen_igniter` (`Descriptor.build/4`), outside this order's path scope.
- `tests` and `chicago_no_mocks` courts remain fixture shell steps; a real `mix test` court
  needs its own DB partition and a candidate carrying tests.
- Graph-side receipt reconciliation (`Descriptor.receipt_from_xaas/2`) is absent from the
  checkout used and was not exercised.
