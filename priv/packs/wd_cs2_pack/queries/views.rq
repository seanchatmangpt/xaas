PREFIX stogaf: <urn:xaas:stogaf:>
PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX dct: <http://purl.org/dc/terms/>
SELECT ?view ?title ?source WHERE {
  ?view a stogaf:View ;
        dct:title ?title ;
        prov:wasDerivedFrom ?source .
}
ORDER BY ?title
